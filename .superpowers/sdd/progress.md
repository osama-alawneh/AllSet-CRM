Task 1: complete (commits 820db51..5dc7955, scaffold; review: low-risk scaffold, verified inline)
Task 2: complete (commit 1968c60, schema; 3 pgTAP pass)
Task 3: complete (commit 39f7a85, RLS admin-only money; 5 pgTAP pass). auth_role() made SECURITY DEFINER to break recursion; granted select on invoices/items to authenticated.
MINOR (defer to Leads/Jobs plan): admins reading leads.quote_value/jobs.price need 'grant select on leads,jobs to authenticated' + a test (rep 0 rows via policy, admin sees). Not yet granted.
Task 4: complete (commit da5acd5, race-safe claim_job; 7 pgTAP pass)
Task 5: complete (commit 0af0926, supabase clients + auth helpers; adapted Next16 async cookies + vitest @ alias)
Task 6: complete (commit 9aca9c9, login + route guard; next build clean, 4 unit tests pass)
Task 7: complete (commit 814e197, seed + 3 login users; 7 pgTAP pass, test ids bumped to 900k to avoid seed collision)
FOUNDATION PLAN COMPLETE (Tasks 1-7). Logins: admin@/rep@/cleaner@clearview.dev pw password123.
FIX: commit f5335fc — grants migration 0004 + seed token fix; E2E verified (logins, RLS, claim-lock all pass). Removed stray CleanView PDF + temp verify script.
PLAN 2 (feat/customers): Task 1: complete (commit f685f24, customer write policies; pgTAP 12 assertions pass, review clean)
Task 2: complete (commits 574cfb7+6989399, Blueprint+ tokens/theme-cookie/shell + placeholders + /leads guard fix; 10/10 unit, build clean, review approved. Minor deferred to final review: layout getRole/getSession redundant round trips + user! assertion; guard duplication across invoices/settings/leads)
Task 3: complete (commit e1e2945, customers list page; 14/14 unit, build clean, review approved. Minor deferred: silent supabase error discard on list page, redundant getRole per page, tr keyboard a11y)
Task 4: complete (commits 259646c+b85f4a5, customer drawer edit/create + tabs + zero-row save fix; 17/17 unit, build clean, review approved. Minor deferred: ?c=0 truthy check, no not-found feedback, disabled-vs-readOnly a11y, unencoded tel/sms/mailto, untested zero-row error branch; pre-existing ThemeToggle lint error noted)
Task 5: complete (commits 56fb26b+a1cafd6, global typeahead + stale-response race fix; 21/21 unit, build clean, review approved. Minor deferred: supabase error discarded in GlobalSearch, no component-level race test)

PLAN 2 VERIFICATION (Task 6, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (5 migrations + seed applied clean).
- `npx supabase test db`: PASS (4 files: schema, rls_money, claim_job, customers_write; 12/12 assertions).
- `npm test`: PASS (21/21 unit tests, 7 files).
- `npm run build`: PASS (clean production build, Turbopack, 10 routes).
- Live drive against `npm run dev` (localhost:3000) + local Supabase, using injected `sb-127-auth-token` auth cookies (base64url-encoded session, `base64-` prefix, per @supabase/ssr default storageKey derived from host `127.0.0.1`) for admin/rep/cleaner sessions obtained via `POST /auth/v1/token?grant_type=password`. Created a local-only `.env.local` (gitignored, not committed) pointing at the local Supabase instance since none existed. 15/15 scripted checks PASS:
  1. admin `/customers`: Sarah Kim row + Invoices column header + per-row inv counts + sidebar Invoices/Settings — PASS.
  2. admin `/customers?c=1` (Sarah Kim) drawer: "CUSTOMER #" badge + "Jobs (" / "Invoices (" / "Leads (" tabs — PASS.
  3. rep `/customers`: no Invoices column, sidebar lacks Invoices/Settings — PASS.
  4. rep `/customers?c=1` drawer: no "Invoices (" tab, has "Save customer" button — PASS.
  5. cleaner `/customers?c=1` drawer: inputs disabled, no "Save customer" button — PASS.
  6. cleaner GET `/invoices` → 307 to `/dashboard` — PASS. rep GET `/invoices` → 307 to `/dashboard` — PASS. cleaner GET `/leads` → 307 to `/dashboard` — PASS.
  7. Unauthenticated GET `/customers` → 307 to `/login` — PASS.
  8. RLS mutation: rep PATCH `customers?id=eq.1` (own-scope write) → 200 + 1 row — PASS.
  9. RLS mutation: cleaner PATCH `customers?id=eq.1` → 200 + 0 rows (RLS-filtered, no error) — PASS.
  10. RLS mutation: cleaner INSERT into `customers` → 403 `42501` — PASS.
  11. Typeahead PostgREST `or()` filter for "sar" (rep token) → returns Sarah Kim (id 1, 142 Maple Ave, 555-0142) — PASS.
- All checks green. No app bugs found; only a test-script regex bug (React SSR comment node `<!-- -->` between number and text) caught and fixed during the run, not an app defect.
- Full command transcript and per-check detail: `.superpowers/sdd/task-6-report.md`. `verify-plan2.mjs` deleted after run per instructions (not committed).
Task 6: complete (commit 435aaaa verification + 7d58b06 lint fixes; pgTAP 12/12, unit 21/21, build clean, lint 0 errors, 15/15 live E2E checks pass). FINAL REVIEW: READY TO MERGE (opus whole-branch). Backlog carried: auth round-trip consolidation, requireRole helper, error UI for failed queries, tr keyboard a11y, ?c=0 latent, not-found drawer feedback, readOnly-vs-disabled, encodeURIComponent quick actions, race/zero-row test coverage, drawer key on customer switch. PLAN 2 COMPLETE.
PLAN 3 (feat/leads-map): Task 1: complete (commits f5c0c06+88c1321, migration 0006 lead writes + won->job trigger + pin RPC with NULL-safe role check + seed rework; pgTAP 24/24 across 5 files, review approved. Minor: trigger fires on no-op status writes — harmless)
Task 2: complete (commit 52224a1, lib/leads + lib/geo pure helpers; 36/36 unit, build clean, review approved, no findings)
Task 3: complete (commit 42056cc, kanban + setLeadStatus; 36/36 unit, build+lint clean, review approved. CARRIED TO TASK 4 (Important): click event fires after drag ends -> onOpen would pop drawer after every drag once ?l= consumer exists; fix in Task 4 via pointer-distance click suppression in LeadCard. Minor: no try/catch around action call in onDragEnd)
Task 4: complete (commits a78d708+6f53753, LeadDrawer + ?l= wiring on /leads + Task 3's carried click-suppression fix in LeadCard; 36/36 unit, build+lint clean. Verbatim brief, no deviations besides the mandated fix.)
Task 4: complete (commits a78d708+6f53753, LeadDrawer + ?l= wiring + post-drag click suppression; 36/36 unit, build+lint clean, review approved. Minor: downPos ref not reset after consume)
Task 5+6: complete (commits 62ae12c+5a5e63a+6ec49a8, map page schematic+mapbox-behind-token + pin RPC action + popover-dismiss fix; 36/36 unit, build+lint clean, review approved. Minor backlog: onPinClick useCallback/ref indirection, marker-sync vs map-recreate effect coupling, PinPopover default status 'won' product decision)

PLAN 3 VERIFICATION (rerun after fix, Task 7, full E2E pass against live local stack, 2026-07-02):
- First pass found a real blocker: reps could never restatus a lead — base leads SELECT is admin-only (protects quote_value) and Postgres requires SELECT-visibility for UPDATE...WHERE, so 0006's leads_update policy was dead code for reps; kanban drag / drawer buttons / won->job silently no-opped. pgTAP missed it (lives_ok passes on 0-row UPDATE). Fixed in f8c4c1e: 0007 set_lead_status SECURITY DEFINER RPC (NULL-safe role check, raises on 0 rows) + actions.ts routes through it; leads_map.sql extended with persistence read-backs.
- Rerun results (all green):
  - `npx supabase db reset`: PASS (7 migrations 0001-0007 + seed clean).
  - `npx supabase test db`: PASS (5 files: schema, rls_money, claim_job, customers_write, leads_map; 31/31 assertions).
  - `npm test`: PASS (36/36 unit, 10 files). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- Live drive (dev server :3000 + injected sb-127-auth-token cookies / bearer tokens, same method as Plan 2): 18/18 checks PASS:
  1. admin /leads: 4 kanban columns + customer names + $ quote on cards — PASS.
  2. rep /leads: renders, no $ amounts anywhere — PASS.
  3. cleaner /leads → 307 /dashboard — PASS.
  4. admin /leads?l=3 drawer: LEAD #0003 badge + $260 quote + status buttons — PASS. rep same: ••••• masked quote + status buttons — PASS.
  5. all roles /map: schematic .mpin pins (10 seeded); cleaner 200 view-only — PASS.
  6. cleaner /map?l=3: drawer present, NO status buttons, NO "Mark won" — PASS.
  7. rep RPC set_lead_status(7,'follow') → 204; admin GET confirms persisted — PASS. cleaner same → 400 P0001 "Not authorized to update leads" — PASS. rep on nonexistent lead → 400 P0001 "Lead 999999 not found" — PASS.
  8. won->job trigger: rep RPC set_lead_status(7,'won') → job created unclaimed; re-won → still exactly 1 job (idempotent) — PASS.
  9. pin RPC: rep create_lead_from_pin('E2E Pin','1 Test Way',42.33,-83.04,'new') → lead id; admin confirms customer+lead with coords — PASS. cleaner → 400 P0001 — PASS.
  10. admin /map pin count 10 → 11 after pin create — PASS.
- Only script-side fix during the run (not an app bug): restrict HTML matching to SSR body (exclude RSC flight payload — literal "$4" ref ids false-matched money regex) + normalize <!-- --> comment nodes (same artifact as Plan 2).
- DB reset to clean seed after run; verify-plan3.mjs deleted; dev server stopped. Full detail: .superpowers/sdd/task-7-report.md.
Task 7: complete (commit f8c4c1e fix + this ledger; pgTAP 31/31, unit 36/36, build clean, lint 0 errors, 18/18 live E2E checks pass). PLAN 3 VERIFIED.
Task 7 + FINAL: complete (verification rerun 18/18 live PASS after set_lead_status fix f8c4c1e; whole-branch review READY TO MERGE. PLAN 3 COMPLETE. Backlog: drop dead leads_insert/update policies+grant (inert defense-in-depth), Mapbox-path useCallback churn + marker/map effect coupling when token lands, onDragEnd try/catch)
PLAN 4 (feat/jobs): Task 1: complete (commit b4a4fd3, migrations 0008-0011 profiles read + claim_job role fix + set_job_status RPC + realtime ping trigger; pgTAP 44/44 across 6 files, review approved, only cosmetic nits)
Task 2: complete (commit 5d3d7df, lib/jobs pure helpers + canTransition matrix; 48/48 unit, build clean, review approved)
Task 3: complete (commit 8291638, jobs board/drawer/actions/page; 48/48 unit, build+lint clean, review approved. CARRIED TO TASK 4: disable claim/drag while action pending (double-click claim shows false 'already claimed' to owner); Minor: .claim.mine styling unused)
Task 4: complete (commits 0915830+9549a8e+ff29720, realtime wiring + pending gate + unmount-race guard; 48/48 unit, build+lint clean, review approved. Minor: board-wide pending disable accepted trade-off)

PLAN 4 VERIFICATION (Task 5, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (11 migrations 0001-0011 + seed clean). Seed jobs confirmed: lead1 claimed by cleaner(333), lead2 unclaimed, lead5 in_progress by cleaner(333), lead8 unclaimed.
- `npx supabase test db`: PASS (6 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board; 44/44 assertions).
- `npm test`: PASS (48/48 unit, 11 files). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- DB-layer RPC matrix (docker exec psql, no native psql on host — used `docker exec supabase_db_ClearViewCRM psql`): rep claim of unclaimed job -> `Not authorized to claim jobs` (blocked) — PASS. cleaner claim of unclaimed job -> returns claimed job row — PASS. cleaner `set_job_status` on OWN claimed job -> in_progress, persisted — PASS. cleaner `set_job_status` on a job they don't own (unclaimed, not theirs) -> `Job <id> not found or not yours` (blocked) — PASS. admin `set_job_status(...,'unclaimed')` on a job clears `claimed_by` to NULL — PASS. NOTE: the brief's own psql script resolves job ids via an inline subquery *inside* the `set role authenticated` cleaner/rep session; since the base `jobs` table's SELECT policy is admin-only (by design, protects `price`), that subquery silently returns NULL under non-admin roles and produces misleading "already claimed"/"NULL not found" errors that are artifacts of the test script, not the app. Fixed by resolving job ids via a superuser (RLS-bypassing) query first, then passing literal ids into the role-scoped calls — this is how the brief's own Step 3 script already does it. Documented here since it could trip up a future rerun of the raw brief text verbatim.
- Claim race (two concurrent `claim_job` calls, same unclaimed job, both cleaner-authenticated, backgrounded via `docker exec ... &`): exactly 1 of 2 logs contains `Job already claimed`; the other returned the claimed row; `claimed_by` set to the winner — PASS.
- Realtime smoke (`scripts/realtime-smoke.mjs`, deleted after run): admin client `setAuth()` + subscribed to private `channel('jobs',{config:{private:true}})`; cleaner client claimed an unclaimed job via RPC in parallel; admin received `broadcast{event:'change'}` with payload `{id:2,status:'claimed'}` — keys checked to be exactly `id,status` (no price/name leak) — PASS.
- Live drive (dev server :3000, cookie-injection `sb-127-auth-token` = `base64-` + base64url(session), same method as Plans 2/3; no browser-automation tool available in this environment so HTTP+cookie fetches against SSR'd pages were used, restricted to the SSR body excluding the RSC flight-payload script tags to avoid false-positive `$` matches): 18/18 checks PASS:
  1. admin `/jobs`: 4 column labels (Unclaimed/Claimed/In progress/Done), `$` price on cards, 🔒 locked pill on claimed job — PASS.
  2. rep `/jobs`: no `$` anywhere in SSR body, no `>Claim<` button — PASS.
  3. cleaner `/jobs`: no `$` anywhere; admin was made to claim lead8's job via RPC mid-run (seed only has one cleaner, so a genuinely "foreign-claimed" job was manufactured) — that job's customer name is absent from cleaner's board (visibleJobs correctly excludes it) — PASS.
  4. cleaner `/jobs?j=<own job>` renders a drawer (`role="dialog"`) with masked price (`•••••`) — PASS. cleaner `/jobs?j=<foreign job>` renders NO drawer — PASS.
  5. rep `/jobs?j=<unclaimed job>` renders a view-only drawer: no "Claim job" button, all status-transition buttons in the `statuspick` block carry `disabled` — PASS.
  6. admin `/leads` (regression check for the 0008 profiles-read-all policy change): still 200, still builds kanban HTML — PASS.
- Not independently re-driven as literal browser clicks/drags (no Playwright/Puppeteer tool available): claim-button click and drag-and-drop UI mechanics. These are thin wrappers — `app/(app)/jobs/actions.ts`'s `claimJob`/`setJobStatus` call `sb.rpc('claim_job'|'set_job_status', ...)` with no extra logic — over the exact RPCs proven correct (including the race and the full authorization matrix) at the DB layer above; `canTransition`/`groupJobsByStatus` drag-gating logic has its own 12/12 unit coverage in `tests/unit/jobs.test.ts`. Two-client live board sync was proven via the realtime smoke script (real supabase-js subscribe/broadcast, not a mock).
- DB reset to clean seed after the run; `verify-plan4.mjs` and `scripts/realtime-smoke.mjs` deleted; dev server process killed. No app bugs found — only the one test-script id-resolution issue documented above (not committed, caught and fixed during the run). Full detail: `.superpowers/sdd/task-5-report.md`.
Task 5: complete (verification pass; pgTAP 44/44, unit 48/48, build clean, lint 0 errors, DB RPC matrix 5/5, claim race 1/1, realtime smoke PASS, 18/18 live E2E checks pass). PLAN 4 VERIFIED.
Task 5 + FINAL: complete (verification 18/18 live + claim race + realtime smoke PASS; whole-branch review READY TO MERGE. PLAN 4 COMPLETE. Backlog: profiles role visible to all (least-priv option: definer RPC/projection), $0 price renders blank on admin card, cleaner backward moves allowed (product call), drawer non-optimistic + redundant refresh, stale 0004 comment, .claim.mine dead CSS)
PLAN 5 (feat/invoices): Task 1: complete (commit 9c297ac, migration 0012 invoice write grants + INV sequence + seed setval; pgTAP 51/51 across 7 files, review approved)
Task 2: complete (commit cfde374, lib/invoices pure helpers; 62/62 unit, build clean, review approved)
Task 3: complete (commit c8d2283, invoices page/table/drawer/print portal + saveInvoice; 62/62 unit, build+lint clean, review approved. Minor backlog: no key on InvoiceDrawer (relied on for create flow), items inputs not pending-disabled, zero-items reseed placeholder line)
Task 4: complete (commit b0aab32, createInvoiceFromJob + JobDrawer active button; 62/62 unit, build+lint clean, review approved)
Task 5: complete (commit e247062, lib/dashboard pure metrics; 73/73 unit total, review approved, date math hand-verified)
Task 6: complete (commit 5588bea feature + commit 03432a2 fix, role-aware dashboard KPIs/chart/claimable-jobs/mini-map + /dashboard revalidate + SchematicMap height prop; 73/73 unit, build+lint clean)

PLAN 5 VERIFICATION (Task 6, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (12 migrations 0001-0012 + seed clean).
- `npx supabase test db`: PASS (7 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board, invoices_write; 51/51 assertions).
- `npm test`: PASS (73/73 unit, 13 files, incl. jobs/invoices/dashboard suites). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- REAL APP BUG found and fixed (within this task's own files, per instructions): the brief's verbatim `<KpiCountUp end={revenue} format={n => fmtMoney(Math.round(n))} />` passes an inline arrow function from the Server Component `app/(app)/dashboard/page.tsx` across the RSC boundary into the Client Component `KpiCountUp` — Next.js forbids non-serializable function props on that boundary (only Server Actions/plain data may cross), causing a 500 on every admin `/dashboard` load (`Functions cannot be passed directly to Client Components...`). Fixed in commit 03432a2: `format` is now a `'money'` string literal resolved by `KpiCountUp` itself via its own `fmtMoney` import; `page.tsx` passes `format="money"` and dropped its now-unused `fmtMoney` import. Re-verified clean (unit/build/lint + live).
- Lint fix (own file, applied inline before first commit): the brief's reduced-motion branch called `setVal(end)` synchronously in the effect body, tripping `react-hooks/set-state-in-effect`; wrapped it in a `requestAnimationFrame` callback (same visual result, satisfies the rule) since the animated path already only calls `setVal` from inside a rAF callback.
- DB-layer invoice matrix (docker exec psql, no native psql on host): admin insert with no `number` → default sequence-backed number assigned, item insert succeeds, rep insert blocked (`42501`), cleaner sees 0 invoices — all PASS mechanically. NOTE: the brief's literal expected value "INV-1004" does not hold when step 11 is run immediately after step 10's `npx supabase test db` (as the brief's own step order prescribes): `invoices_write.sql`'s pgTAP test consumes 2 values off the shared `invoice_number_seq` (one successful admin insert + one rep insert whose `number` DEFAULT is computed before its RLS `WITH CHECK` rejects the row) — Postgres does not roll back sequence advances on transaction ROLLBACK, so `pg_prove`'s per-file `begin;...rollback;` does not undo it. Confirmed empirically (sequence 1003→1005 across one `test db` run, main DB not an isolated shadow). Result was `INV-1006` instead of `INV-1004` — not an app defect, an artifact of running the psql matrix right after `test db` in the same persistent local DB; the generation mechanism (unique, monotonic, no collision with seeded INV-1001..1003) is correct. DB reset afterward per brief.
- Live drive (dev server :3000, cookie-injection `sb-127-auth-token` = `base64-` + base64url(session) via `POST /auth/v1/token?grant_type=password`, same method as Plans 2-4; no browser-automation tool available so canvas redraw-on-theme-toggle, drag/print-dialog, and literal button clicks were not independently driven — see note below): 45/45 scripted checks PASS, incl.:
  1. admin `/dashboard`: 200, Revenue·MTD + Overdue-invoices KPI cards present, `<canvas>` (RevenueChart) present, Jobs/week + Win rate + Claimable jobs + Neighborhood snapshot present, `$` figures present. KPI values cross-checked from the RSC flight payload against seed data: win rate 80% (4 won / 1 lost), overdue $165 (INV-1003: 140+25, issued 2026-05-28, >30d before server "today" 2026-07-02), jobs/week 3, revenue MTD $0 (correct: seed's only `paid` invoice, INV-1001, was issued in June, current month is July — `revenueMTD` is unit-tested to return 0 for this case, not a bug).
  2. rep + cleaner `/dashboard`: 200, NO "Revenue · MTD", NO "Overdue invoices", `•••••` placeholder present, NO `<canvas>`, **zero** `$[0-9]` matches in the SSR body, Jobs/week + Win rate + Claimable jobs still present — PASS for both roles.
  3. rep + cleaner GET `/invoices` → 307 to `/dashboard` — PASS.
  4. admin `/invoices`: 200, table lists INV-1001/1002/1003 — PASS.
  5. Invoice CRUD via PostgREST (admin bearer token): insert with no `number` → 201 + auto-assigned `INV-\d+` ≥ 1004 — PASS; insert `invoice_items` — PASS; read-back finds the row — PASS; PATCH status → 200 — PASS; DELETE item then invoice (cleanup) — PASS.
  6. Invoice denial via PostgREST: rep + cleaner insert → 403 (`42501`) — PASS; rep + cleaner select → 0 rows (RLS) — PASS for both.
  7. Claim-flow revalidate: cleaner `claim_job` RPC on an unclaimed job → success; `/dashboard` refetch reflects the change (claimable list changed) — PASS, confirming the new `revalidatePath('/dashboard')` addition in `claimJob`/`setJobStatus` is wired correctly end-to-end.
- Not independently browser-driven (no Playwright/Puppeteer tool in this environment, consistent with Plans 2-4's documented limitation): RevenueChart canvas redraw on theme toggle, KPI count-up animation + reduced-motion jump (both are thin rAF/DOM effects with their own unit-testable pure math — cubic ease-out — already covered; the underlying `end` prop values were cross-checked above via the RSC payload), InvoiceDrawer line-item editing/`+Add line`/status-badge-color JS interactions and the `🖨 Print PDF` browser print dialog (unchanged since Task 3, out of this task's diff surface), MiniMap pin-click → `/map?l=<id>` vs background-click → `/map` (both are trivial `router.push` calls wrapping already-proven navigation, no new logic).
- DB reset to clean seed after the run; `verify-plan5.mjs` and `check-kpi.mjs` deleted after use (not committed); dev server process killed. One real app bug found and fixed (RSC function-prop serialization, see above) plus one lint fix (set-state-in-effect), both within this task's own new files — no pre-existing app code touched. Full detail: `.superpowers/sdd/task-6-report.md`.
Task 6: complete (verification pass; pgTAP 51/51, unit 73/73, build clean, lint 0 errors, DB invoice matrix 4/4 mechanically correct (number-format deviation explained, not a defect), 45/45 live E2E checks pass). PLAN 5 VERIFIED.
Task 6: complete (commits 5588bea+03432a2+e675d01, dashboard page/components + revalidate tweaks + 45/45 live verification; 73/73 unit, pgTAP 51/51, build+lint clean, review approved. Minor: sequential awaits on dashboard page — brief-mandated)
FINAL: PLAN 5 READY TO MERGE (whole-branch review clean). Backlog: dashboard Promise.all, tax/deposit alignment between revenueMTD and invoiceTotal when Phase 3 lands, jobsThisWeek trailing-window semantics = product question for client, InvoiceDrawer key/pending-disable/placeholder-line nits. PLAN 5 COMPLETE.
PLAN 6 (feat/exports-pwa): Task 1: complete (commit 96261f8, role-aware CSV exports; 84/84 unit, build+lint clean, review approved, no findings)
Task 2: complete (commit 36a32b2, PWA manifest/icon/sw/register/offline; 84/84 unit, build+lint+prod-curl clean, review approved. Minor backlog: sw caches non-ok static responses until version bump; sw.js cache-control header note for prod deploy)
Task 3: complete (commits 28167f7+9c9229f, a11y pass + inner-control keydown guard + dead CSS removal; 84/84 unit, build+lint clean, review approved)
Task 4: complete (commit d62c5ca, dashboard Promise.all; 84/84 unit, build+lint clean, review approved)

PLAN 6 VERIFICATION (Task 5, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (12 migrations 0001-0012 + seed clean; no DB objects changed this plan).
- `npx supabase test db`: PASS (7 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board, invoices_write; 51/51 assertions — no regression).
- `npm test`: PASS (84/84 unit, 14 files). `npm run build`: PASS (clean, 10 routes incl. /manifest.webmanifest, /offline). `npm run lint`: PASS (0 errors).
- `npx vitest run tests/unit/csv.test.ts`: PASS (11/11) — the authoritative role-omission specs all green: leadsCsvTable/jobsCsvTable/customersCsvTable include Value/Price/Invoices only for admin (header+cell both omitted for non-admin, not blanked); invoicesCsvTable Total spec; csvEscape formula-injection guard; toCSV BOM+CRLF.
- Headless PWA probes (`npm run build && npm run start`, curl): `/manifest.webmanifest` 200 with `ClearView CRM`, `display:standalone`, `theme_color:#2f6df6`, icons array — PASS. `/sw.js` contains all 4 allowlist markers (`clearview-v1`,`navigate`,`/_next/static/`,`/offline`) — PASS. Manual review of the full sw.js confirmed the default-deny contract intact: non-GET early-return, cross-origin early-return, navigate network-only w/ `/offline` catch-fallback (HTML never cached), static-asset allowlist cache-first, else no interception. `/offline` 200, `/icon.svg` 200 `image/svg+xml` — PASS.
- Live CSV drive: Export is 100% client-side (`downloadCSV`/`toCSV` called directly in the table components, no server route) — no browser-automation tool available (same limitation as Plans 2-5) to drive an actual click->save->open-in-Excel round trip. Verified as a proxy: `⬇ Export CSV` button present in SSR HTML for every role/route that should show it (admin/rep/cleaner customers+jobs, admin/rep leads, admin invoices), combined with the Step 2 unit-tested builder role-omission logic — treated as equivalent coverage per prior-plan precedent for file-download flows.
- Live regression sweep (`verify-plan6.mjs`, ad-hoc/deleted, dev server + cookie-injected sb-127-auth-token sessions, same method as Plans 2-5): 93/93 scripted checks PASS — every route 200s for permitted roles; rep/cleaner `/invoices`+`/settings`->307 `/dashboard`; cleaner `/leads`->307 `/dashboard`; zero `$[0-9]` leaks in rep/cleaner dashboard/customers/leads/jobs SSR bodies (RSC flight payload stripped to avoid false positives); cleaner dashboard shows `•••••` mask + no Revenue-MTD/Overdue KPI cards (rep same, no mask check needed); admin dashboard has `<canvas>` + `$` KPIs, admin leads/jobs show `$` values; skip-link + `#main` landmark present on every route; `aria-current="page"` on active nav for all 3 roles; `aria-pressed` confirmed on LeadDrawer (admin `/leads?l=3`) and JobDrawer (admin `/jobs?j=1`) statuspick buttons (1 true + 3 false each).
- Not independently browser-driven (no Playwright/Puppeteer tool, consistent with every prior plan): literal Tab-key skip-link reveal, real Enter/Space row activation on customers/invoices tables (onKeyDown handlers invisible to SSR HTML, unchanged since Task 3, no new logic this task).
- RESIDUAL MANUAL CHECKS (browser-only, documented for the user, non-blocking): (1) Chrome DevTools -> Application -> Manifest shows "ClearView CRM" + installable; (2) Service Workers panel shows `/sw.js` activated; (3) DevTools "Offline" toggle + navigate renders `/offline`; (4) "Add to Home Screen" installs a standalone window; (5) iOS Safari ignores the SVG manifest icon — `apple-touch-icon` PNG is a documented post-MVP follow-up, not added here.
- No app bugs found; no application code touched. DB reset to clean seed after the run; dev/prod server processes killed; verify-plan6.mjs (scratchpad, not committed) deleted from working consideration. Full detail: `.superpowers/sdd/task-5-report.md`.
Task 5: complete (verification pass; pgTAP 51/51, unit 84/84, build clean, lint 0 errors, csv role specs 11/11, headless PWA probes clean, 93/93 live E2E checks pass). PLAN 6 VERIFIED. MVP COMPLETE (Plans 1-6 all verified).
Task 5 + FINAL: complete (93/93 live + PWA headless checks; whole-branch review READY TO MERGE. PLAN 6 COMPLETE — MVP DONE. Backlog: sw res.ok guard, sw.js Cache-Control at deploy, csvEscape prefixes any leading -/+/=/@ value not just injection)

PHASE 1.5 — PLAN 7 (feat/auth-admin):
Task 1: complete (commit 73a86b5, Blueprint+ login + dark theme default flip; 84/84 unit, lint+build clean, review approved, no findings)
Task 2: complete (commit b2d3ec3, sidebar sign-out; 84/84, lint+build clean, review approved. Minor backlog: SignOutButton stuck-pending if auth.signOut() rejects — plan-mandated verbatim code, add try/finally or unconditional redirect)
Task 3: complete (commit e6e1fae, parseNewUserForm TDD; 89/89, lint+build clean, review approved, no findings)
Task 4: complete (commits c6d9655+ea91a07, service-role client + createUser/setUserRole actions + orphan-rollback fix; 89/89, lint+build clean, review approved after fix. Minor backlog: opaque non-null env assertions in lib/supabase/admin.ts)
Task 5: complete (commits f1c4703+499d6e5, settings users page + UsersPanel + migration 0013 service_role profiles grants — live check caught real runtime permission bug in T4 actions, migration was the fix; 89/89, lint+build clean, 12/12 live checks, review approved. NOTE: plan 8 task 1 migration must renumber to 0014)
Task 6 + FINAL: complete (fable whole-branch review READY TO MERGE; server-only guard added 63cad36; merged to main. Backlog: pgTAP grant assertions, normalizeRole on settings page p.role, ThemeToggle SSR label wrinkle, SignOutButton reject path, last-admin protection, listUsers 200 cap. PLAN 7 COMPLETE)

PLAN 8 (feat/crud-datamodel):
Task 1: complete (commit 5b2650c, migration 0014 [renumbered from brief 0013] + crud_rpcs pgTAP; pgTAP 80/80, 89/89 unit, lint+build clean, review approved. Minor backlog: delete not-found raise unasserted in pgTAP; set_updated_at not definer [harmless])
Task 2: complete (commits 76df4bb+ef9b260, Tabs type=button root-cause fix + minirow deep-links + keyboard a11y rowNav; 89/89, lint+build clean, review approved after a11y fix)
Task 3: complete (commit ed9035e, Lead/Job types + parseLeadForm/parseJobForm TDD + CSV Description column + build-forced select/literal threading through dashboard/jobs/leads/map pages [reviewed: no logic change, no money leak]; 98/98, lint+build clean, review approved. Minor backlog: parseJobForm date format-only validation)
Task 4: complete (commit e09be12, six lead/job CRUD server actions wrapping 0014 RPCs, revalidatePaths verified, param names cross-checked to migration; 98/98, lint+build clean, review approved, no findings)
Task 5 + FINAL: complete (fable whole-branch review READY TO MERGE, merged to main. Backlog: PRE-EXISTING 0006 grant gap — rep can blind-write quote_value via direct PostgREST insert/update on base leads (no read-back possible; integrity not leak) → follow-up migration to column-scope grants; jobs/actions.ts money comment overstates parser role; won-job description not re-synced on lead edit; pgTAP roleless coverage for job RPCs; delete_job invoice SET NULL untested. PLAN 8 COMPLETE)

PLAN 9 (feat/drawer-ux):
Task 1: complete (commit c3a64fc, LeadDrawer read/edit/create/delete; 98/98, lint+build clean, 26/26 SSR checks, review approved. Minor backlog: edit-form Description placement [plan-mandated], LeadDrawer missing key on lead switch [latent])
Task 2: complete (commit 4853af7, JobDrawer details+lead quick-view+admin CRUD; 98/98, lint+build clean, live per-role SSR pass, review approved. Minor backlog: $0 renders as dash [pre-existing]; seed jobs null description [seed-only])
Task 3: complete (commit 20b67ab, CustomerDrawer read-only default + Edit toggle; 98/98, lint+build clean, live SSR pass, review approved. Controller-resolved warning: cleaner ?new=1 shows create form but RLS [0005] blocks insert — UX nit backlog. Minor: raw enum type display [plan-mandated])
Task 4: complete (commit 15df1f8, InvoiceDrawer placeholders + read-only mode + empty-line drop rule; 99/99, lint+build clean, review approved. Minor backlog: no router.refresh after edit-save [plan-mandated], no remove-line control [pre-existing])
Task 5 + FINAL: complete (battery green [pgTAP 80/80, unit 99/99, lint, build], 67/67 live walkthrough, fable review WITH FIXES -> drawer key fix 891b49b applied, merged to main. Backlog: customers ?new=1 cleaner gate, invoices isNew&&!selected, admin cannot clear money via forms [RPC coalesce semantics], enum label + $0 display, JobDrawer/LeadDrawer minirow keyboard nav, won-job desc resync. PLAN 9 COMPLETE)

PLAN 10 (feat/search-views):
Task 1: complete (commit e163e73, buildEntityOrFilter/filterLeads/filterJobs/filterInvoices/SearchHit/hitHref TDD; 106/106, lint+build clean, review approved, no findings)
Task 2: complete (commit 97ba38d, grouped role-aware global search, 4-way fanout w/ shared cancel token; 106/106, lint+build clean, live RLS role checks, review approved. Minor note: cleaner leads gate client-side only [money-free view, pre-existing architecture])
Task 3: complete (commit 381d32b, local filters on leads/jobs boards + invoices table; CSV full-export + drag-state integrity verified; 106/106, lint+build clean, review approved. Minor backlog: empty-state wording, kanban no-match state)
Task 4: complete (commit 8e560f0, board/list toggle + LeadsListTable/JobsListTable + view-preserving backTo; 106/106, lint+build clean, live role checks, review approved. Minor backlog: useJobsRealtime hook extraction [plan-mandated copy], th scope=col, table captions)
Task 5 + FINAL: complete (battery green [pgTAP 80/80, unit 106/106, lint, build], live 3-role walkthrough PASS, fable review READY TO MERGE no fixes, merged to main. Backlog: dropdown reopen after dismiss, Enter stale first-hit, buildEntityOrFilter empty-fields guard, search hits drop ?view=list, leads not searchable by customer name [plan choice], fmt/day dedup, pgTAP view-money-column guard assertion, useJobsRealtime hook, th scope/captions. PLAN 10 COMPLETE — PHASE 1.5 DONE, ALL 4 PLANS MERGED)

HARDENING WAVE (feat/hardening, post-phase-1.5):
Hardening: complete (commit 18262fa merged, migration 0015 revokes table-level leads insert/update, re-grants all columns except quote_value+id; pgTAP 84/84 [+4 guards: views have no money columns, rep 42501 on quote_value write], 106/106 unit, lint+build clean, review approved. Minor backlog: same-file positive-control lives_ok in rls_money.sql; status grant kept for leads_map.sql legacy test)
Polish wave: complete (commits 4661115..ee98b52 merged — useJobsRealtime extraction, th scope+aria-label, conditional empty states, customers new=1 cleaner gate, invoices isNew&&!selected; 106/106, lint+build clean, review approved, no findings)
Money-clear: complete (commit 907bc2d merged, blankMoneyToZero form-boundary helper, user-decided semantics; 111/111, lint+build clean, RPC live check, review approved. Minor backlog: money-form test name over-claims parser integration)
User decisions 2026-07-06 (2nd batch): blank money = clear to 0 [done]; won-job description stays copy-on-create [no change]; search hits keep board target [no change].
Cookie-refresh fix: complete (commit 04dbaed merged — proxy.ts session refresh [Next 16 renamed middleware->proxy] + setAll try/catch; repro BEFORE 500/AFTER 200 with rotated refresh token; 111/111, lint+build clean, PWA assets intact, review approved. User-found bug during test run: stale browser session hit refresh path our fresh-session live checks never exercised. Minor backlog: anchor matcher lookahead terms)
WAVE 1 (branch wave1-security-perf-pwa): Task 1: complete (commits 61ebb6c..e604dd8, SEC-1..4 migration 0016 + pgTAP pins; review clean). Minor deferred to final review: SEC-4 pins only created_by (not created_at/updated_at); no explicit rep/admin branch assertion for jobs_public.
Task 2: complete (commit 6343f2a, cache() auth + parallel page queries + logQueryError; 111/111, build clean, review approved). Minor deferred to final review: getRole logs via raw console.error not logQueryError (plan-mandated); customers drawer detail reads (page.tsx:84-107) left sequential though independent.
Task 3: complete (commits a65a721+07bfaa8, iOS PWA icons/meta/manifest + legacy apple-capable tag via metadata.other; 111/111, build clean, review approved, no open findings).
WAVE 1 COMPLETE (5 commits 61ebb6c..07bfaa8 on wave1-security-perf-pwa). Exit checklist all green: pgTAP 8/8 (91 asserts), vitest 111/111, lint+tsc+build clean, REST claim_job as cleaner returned bare id, jobs_public money-free + row-filtered, manifest JSON valid w/ PNG+maskable, served HTML has apple-mobile-web-app-capable + viewport-fit=cover. Final whole-branch review (fable): ready to merge, 0 Critical/Important.
Wave 2 riders from final review: rep-arm pgTAP assertion for jobs_public (only untested branch of view predicate); logQueryError sweep for dashboard/settings pages; themeColor unconditional dark vs cookie light theme; maskable icon 80%-width square may clip circular mask (check DevTools preview); fold getRole console.error into logQueryError when touching lib/auth.ts.
NOTE: local Supabase 54xxx ports blocked by Windows excluded-port ranges (54235-54435); stack currently runs on 55xxx while config.toml says 54xxx — remap config temporarily for test:db, revert before commit (or reboot/winnat reset to reclaim 54xxx).

WAVE 2 (branch wave2-contrast-touch-keyboard, base bf89f10, plan docs/superpowers/plans/2026-07-07-wave2-contrast-touch-keyboard.md):
Task 1: complete (commit aa11bb5, contrast tokens/--on-status/light ramp AA; 111/111+lint+tsc, review approved, no findings. Minor: live two-theme eyeball deferred to wave exit).

Task 2: complete (commit 93c224a, mobile CSS 16px/dvh/safe-area/44px/pin hit; 111/111+lint+tsc, review approved. Minor for final review: safe-area comment says "inert until viewport-fit=cover ships" but Wave 1 already shipped it — plan-mandated verbatim, fix wording).
Task 3: IMPL d429137 REVIEW FOUND CRITICAL — cards forward only onPointerDown but Mouse/Touch/Keyboard sensors activate on onMouseDown/onTouchStart/onKeyDown, drag dead on all inputs. Fix dispatched to implementer (spread {...listeners} on root + title-button mousedown/touchstart stopPropagation). Re-review after fix. Important plan-mandated finding deferred to human: nested interactive (title <button> inside role=button draggable root) — plan explicitly mandates structure; present at wave end. Minor: no render-level card tests (why 111/111 missed it).
Task 3: complete (commits d429137+f5c6f0d, Mouse/Touch/Keyboard sensors + cardlink title buttons + touch-action manipulation + cooperativeGestures; Critical drag-dead regression caught by review, fixed via {...listeners} spread + per-event stopPropagation, re-review approved. Minor for final review: long-press drag can start from Claim button [touch, negligible]; no render-test infra so live keyboard/touch drag check MANDATORY at exit. Important plan-mandated ESCALATE TO HUMAN: nested title <button> inside role=button draggable root — widget-in-widget AT pattern, plan mandates it).
WAVE 2 FINAL: fable whole-branch review WITH FIXES -> f4252ed applied (user-select suppression, title pointerdown bubbles, safe-area comment), reviewer CONFIRMED ready to merge PENDING HUMAN MANUAL CHECKLIST. Battery green post-fix: lint+tsc+111/111+build. Branch wave2-contrast-touch-keyboard, 5 commits aa11bb5..f4252ed, NOT MERGED — user does manual phone/theme checklist first. Backlog from final review -> Wave 3: drag-handle restructure via setActivatorNodeRef (resolves nested-button ARIA violation + title drag dead zone + claim long-press leak); darken light --lost to ~#c93b3b (white-on-#d64848 4.3:1 fails AA small text, plan-itself error); render-test smoke infra (would have caught the drag-dead Critical); invoice .num inputs 16px layout check on phone. Wave-1 riders still open (rep-arm pgTAP, logQueryError sweep, themeColor cookie, maskable icon preview, getRole console.error). Manual checklist: dark Follow-up selected = dark-on-amber; light kanban headers/money AA; phone swipe-scroll vs long-press drag + Claim button touch; keyboard full drag cycle + title Enter opens drawer; inputs no iOS zoom; map one-finger scroll hint; kanban text now unselectable on desktop (accepted side effect).
USER CONFIRMED 2026-07-07 (screenshot, phone view during Wave 2 checklist): sidebar covers half the viewport <=860px — MOB-H2 hamburger nav is top priority for Wave 3 (already plan Task 2, Drawer-based slide-in; user said "slide down" — pattern already decided as off-canvas Drawer, functionally equivalent).
WAVE 2 MERGED to main 2026-07-07 (merge fce53da, branch deleted). User manual checklist PASSED (incl. real phone view). User-confirmed MOB-H2 priority for Wave 3.
Wave 3 plan amended 2026-07-07 (user-approved): +Task 7 drag-handle restructure, +Task 8 riders sweep (Wave 1 riders + --lost AA + render-test infra). Plan now 8 tasks.

WAVE-A (branch wave-a-shell-a11y, plan 2026-07-08-wave3-map-combined.md Phase A):
Task 1: complete (commit fbb411a, Drawer focus trap/restore/labelId/scroll-lock + 4 callers; lint+tsc+111 unit pass, review approved. Minor deferred: zero-focusable trap edge, static labelId collision if concurrent same-entity drawers)
Task 2: complete (commits 12a791c+9af53ab, MobileNav hamburger drawer + Topbar nav slot + <=860px CSS; review found Critical hamburger display:none cascade bug, fixed 9af53ab, re-review approved; lint+tsc+111 unit pass. Minor deferred: MobileNav duplicate usePathname, iconbtn no min-width 44px (pre-existing). Manual <=860px browser check batched to phase-end checklist)
Task 3: complete (commits 4b8229a+cccdc3a, ARIA tabs roving tabindex/arrow-nav + label prop; review found Critical all-panes-visible (author .tabpane.on beats UA [hidden]), fixed cccdc3a via .tabpane[hidden] rule, re-review approved; lint+tsc+111 unit pass. Minor deferred: getElementById vs refs, tabs[NaN] on empty tabs (unreachable), keyboard tab check batched to phase-end checklist)
Task 4: complete (commit 7bfb6b6, GlobalSearch ARIA combobox + orderedHits flat nav + explicit-Enter behavior + .scard.active; review approved, no fixes. Minor deferred: duplicated group-filter scans, listbox group headings lack role=group, no RTL test for keyboard nav; manual search keyboard check batched to phase-end)
Task 5: complete (commit 93a324c, app/(app)/loading.tsx skeleton + error.tsx; Next 16 deviation verified: error boundary prop is unstable_retry (16.2.0+, reset still supported) — docs-confirmed; lint+tsc+111 unit pass, review approved. Minor deferred: no console.error/telemetry hook in error.tsx; manual slow-3G/thrown-error check batched to phase-end)
Task 6: complete (commits ed6978b+412c9e7, polish sweep: lib/rowNav extract + form-err/role=alert x8 files + PinPopover escape/clamp + ThemeToggle SSR initial + canCreate gate + UsersPanel labels + KPI auto-fit + offline dark; review found Important CustomerDrawer error p missing role=alert w/ false skip rationale, fixed 412c9e7, re-review approved; lint+tsc+111 unit+build pass. Minor deferred: KPI 3+1 orphan tile ~570-760px (plan-accepted), dead suppressHydrationWarning on ThemeToggle)
Task 7: complete (commit 002c50b, drag-handle activator on LeadCard/JobCard + .draghandle CSS; Step 2 choice: title keeps mousedown/touchstart stops (non-draggable dead zone); var(--mut)->var(--muted) token fix; pointer-only listener subset on root verified load-bearing for KeyboardSensor targeting; lint+tsc+111 unit pass, review approved, no fixes. Minor deferred: click-on-handle-no-drag opens drawer (consistent w/ card body), pointerListeners not memoized, live keyboard-drag check batched to phase-end)
PHASE A TASKS 1-7 COMPLETE. Whole-branch review next.
WHOLE-BRANCH REVIEW (fable): initial verdict "with fixes" — 1 Important: GlobalSearch Enter/arrows active while listbox closed (visible guard dropped in combobox rewrite). Fixed 27117b5 (Enter requires visible, active resets on Escape/outside-click, ArrowDown reopens). Re-verified: READY TO MERGE.
Backlog carried to Task 13 riders sweep: Drawer FOCUSABLE not filtering [hidden] subtrees (priority — Tabs panes inside CustomerDrawer), drag handle rendered when draggable=false (gate it), form-err grep missed UsersPanel:47 + LoginForm:39 inline styles (DRY only), MobileNav aria-haspopup=dialog + drawer-open-across-resize edge, two-dialog Escape/trap collision landmine (unreachable today), error.tsx console.error hook, GlobalSearch role=group headings + dup group scans, KPI 3+1 orphan (plan-accepted), dead suppressHydrationWarning, .iconbtn min-width coarse.
Wave-end npm run build: PASS (all routes compile). PHASE A COMPLETE pending manual checklist + merge to main.

WAVE-B (branch wave-b-map-pins-search, base 91c668c, plan Phase B Tasks 8-12):
Task 8: complete (commit 68fefaf, lib/mapPins MapPin union + buildMapPins + pinColor/pinKey; 117/117 unit, lint+tsc clean, review approved, no findings. Minor noted: old Pin type in lib/leads coexists until Task 10 reconciles)
Task 9: complete (commit 7bd63dd, lib/geocode v6 URL builder + defensive parser; 123/123 unit, lint+tsc clean, review approved, no findings)
Task 10: complete (commit b1e3cd0, button pins both impls + job circles + Legend + MiniMap adapt + dashboard map link + schematic chrome + MapView/page bridge; 123/123 unit, lint+tsc+build clean, review approved, no fixes. Minor deferred to final review: focus-visible rule duplicates global, -moz-focus-inner reset absent, dark .lg-head off-token fallback [plan-mandated], dashboard inline legend still lists LOST [stale, out of task scope])
Task 11: complete (commit 65a76f3, map page role-split jobs fetch + visibleJobs-gated pins/drawer + ?j= JobDrawer + ?l= precedence; 123/123 unit, lint+tsc+build clean, review approved, no findings. Minor: role&& guard dead weight [TS narrowing], leadDetail .single() logs on missing lead [inherited from jobs page])
Task 12: complete (commit 488a5b6, MapSearch combobox + flyTo/seq temp marker + layer toggle chips + searchbox/chip CSS; 123/123 unit, lint+tsc+build clean, review approved, no fixes. Two lint-driven deviations judged behavior-preserving [queueMicrotask reset branch, empty-li role=option — latter strict improvement]. Minor: two different set-state-in-effect workarounds same commit, aria-controls id absent while closed [plan-mandated pattern])
PHASE B TASKS 8-12 COMPLETE. Whole-branch review next.
WHOLE-BRANCH REVIEW (fable): READY TO MERGE, 0 Critical/Important. New backlog: MapView popover dismissal lead-only — job-pin click leaves create-popover pending (PLAN DEFECT, track openId incl. j:); MapSearch no res.ok check (401/429 reads as 'No results'; failed state set but never rendered distinctly); MapSearch input accname placeholder-only (fold into Task 14 SR spot check); onFocus reopens stale listbox. Carried 9 per-task minors all non-blocking (triage: dashboard inline legend LOST is self-consistent — dashboard pins don't exclude lost; alignment decision deferred).
PHASE B COMPLETE pending user manual map checklist + merge to main. Branch wave-b-map-pins-search, 5 commits 68fefaf..488a5b6.
WAVE-B MERGED to main 2026-07-08 (merge 685d5d3, branch deleted). User manual checklist PASSED (pins keyboard, search flyTo, toggles, cross-role job pins).

WAVE-C (branch wave-c-riders, base 1a7f17d, plan Phase C Tasks 13-14):
Task 13: complete (commits cfe475e+8ad8136, rep-arm pgTAP 92/92 + logQueryError sweep incl getRole + generateViewport theme-cookie themeColor + maskable 410->280px + light --lost #c93b3b + render-smoke infra jsdom docblock, 131/131 unit; review approved, no fixes. Minor deferred to final review: RTL cleanup() no-ops (no globals:true / afterEach) — latent only, tests scope to container; .kpi .bad on --paper would be 4.31:1 [likely panelled]. Manual: maskable DevTools preview batched to exit checklist)
Task 13b (controller-scoped A/B backlog riders): complete (commit 64915d7, 12 riders: Drawer [hidden] trap filter, draghandle gated on draggable both cards, form-err DRY, MobileNav aria-haspopup, error.tsx console.error, GlobalSearch role=group + single-pass memo, dead suppressHydrationWarning, .iconbtn min-width, MapView job-pin popover dismissal [setPending(null) in onPinClick — disclosed deviation, traced sound], MapSearch res.ok+failed render / stale-focus ref guard / aria-label; 133/133 unit, lint+tsc+build clean; review approved, no fixes. Minor deferred to final review: .iconbtn min-width in base rule not coarse-pointer block [convention drift, negligible today], no JobCard gating smoke test, no MapView rider-9 regression test)
Task 14 Step 1 battery: lint clean, tsc clean, 133/133 unit (22 files), build compiles, pgTAP 8 files/92 asserts — ALL GREEN (controller-run 2026-07-08).
WHOLE-BRANCH REVIEW (fable): READY TO MERGE, 0 Critical. 2 Important: ThemeToggle theme-color meta stale after toggle (FIXED d5e1581 + geocode http diagnostic Minor #7); MapSearch reopen-after-pick listbox (PRE-EXISTING, carried as named rider). Deferred-minor triage: all 5 CARRY (.kpi .bad verified non-issue — all --lost text on --card 4.90:1; legend swatch non-text 3:1 ok). Named riders for next wave: MapSearch pick() reopens listbox ~300ms (pickedRef guard); render-test hygiene (clearAllMocks + cleanup afterEach + useDraggable disabled:true assertion) when files next touched; GlobalSearch group label double-announce (aria-hidden on visual label); LoginForm form-err margin fold; maskable icon designed variant ~65-70% w/ bleed (280px conservative-small on rounded-square masks). NOTE: task-13 brief's "--lost on paper ≈4.6:1" estimate was wrong (actual 4.31:1) — don't cite forward; moot since no --lost text on paper.
Final reviewer confirmed d5e1581 (verdict stands, ready to merge). WAVE-C MERGED to main 2026-07-08 (merge 9573873, branch deleted). Post-merge tests+tsc green. PHASE C COMPLETE. Manual A-C exit checklist batched to user (Task 14 Steps 2-3 + maskable DevTools preview); straggler-fix commit slot (Step 4) open if checklist finds issues. Phases D-E next (Tasks 15-23).
User manual A-C exit checklist PASSED 2026-07-08 (cross-role map, keyboard/SR, theme toggle chrome sync, maskable preview). No stragglers. Phase D (Tasks 15-18) next.

WAVE-D (branch wave-d-owner-quickwins, base f14f924, plan 2026-07-08-wave3-map-combined.md Phase D Tasks 15-18):
Task 15: complete (commits adce8c6+a9c7534, number spinners off, stories/panes default 0 [optNum already ok — regression test in lead-form.test.ts], autofill off/new-password, CopyButton x3 drawers; review found Important .qa flex-stretch regression, fixed a9c7534 [.qa align-items:center + copybtn 44px into pointer:coarse list], re-review approved; 134/134, lint+tsc clean. Minor deferred to final review: .qa a anchors absent from coarse 44px list [pre-existing], CopyButton label-in-name WCAG 2.5.3 [brief-specified], CopyButton null vs anchors render on empty phone)
Task 16: complete (commit 1f167ea, migration 0017 waived+cancelled enum, InvoiceStatus widened, colors --follow/--lost, drawer/table already import array [verified no-op], pgTAP no enum assertions; RED->GREEN, 135/135, pgTAP 92, lint+tsc+build clean, review approved, no fixes. Minor deferred: invoiceStatusLabel unconsumed by UI [pre-existing raw-lowercase pattern], no color-value assertions for new statuses)
Task 17: complete (commits 3532768+ae58455, SERVICE_TYPES const + spec, service input->select w/ legacy option both drawers; review found Important required-attr regression [plan-snippet omission], fixed ae58455, re-review approved; 136/136, lint+tsc clean. Minor deferred to final review: select block duplicated across drawers [brief-mandated], SERVICE_TYPES lives in lib/leads for jobs too [move if 3rd consumer]. Manual [4 options + legacy selected] batched to phase-end checklist)
Task 18: complete (commit 68a7ba9, migration 0018 scheduled_date->timestamptz + jobs_public verbatim recreate [char-identical to 0016, no security_invoker, grant re-issued] + create_job/update_job RPCs widened date->timestamptz [disclosed deviation — date param would silently truncate time; opus reviewer diffed bodies vs 0014 line-by-line, faithful] + datetime-local input + parser regex widened + dayTime() 5 render sites; pgTAP 92 before+after, 142/142 unit, lint+tsc+build clean, review approved, no fixes. Minor deferred: timezone coupling assumes UTC session [inherent to string-slice convention], CSV scheduled_date now full ISO [visibility only])
PHASE D TASKS 15-18 COMPLETE. Whole-branch review next.
WHOLE-BRANCH REVIEW (fable): WITH FIXES -> 7e976b5 applied (CustomerDrawer invoiceStatusColor import [Important, plan defect — Task 16 checklist omitted CustomerDrawer], .qa a into coarse 44px list, waived/cancelled color assertions, csv dayTime, parser msg). Reviewer CONFIRMED: READY TO MERGE. Controller battery green post-fix: lint+tsc+144/144+pgTAP 92+build all routes. Branch wave-d-owner-quickwins 7 commits adce8c6..7e976b5, NOT MERGED — awaiting user manual checklist. CARRY backlog: CopyButton copied-state accname/aria-live, CopyButton null vs dead anchors on empty phone, invoiceStatusLabel unconsumed [wire w/ Tier-3 money pass], ServiceSelect extract + SERVICE_TYPES neutral module on 3rd consumer, timezone wall-clock UTC coupling [doc in ARCHITECTURE.md, also stale lines 107/175], .qa a text ~px above center in 44px box [cosmetic], future hardening: explicit revoke jobs_public from anon.
AM/PM rider (user request during Phase D checkist review): complete (commits 29c5d7a+43d7fb7, dayTime 12-hour AM/PM display all sites+CSV [string-slice convention kept, datetime-local input untouched — widget format follows OS locale]; review found Important missing 01-11 AM test bucket, fixed 43d7fb7 [verified in commit by controller]; 148/148, lint+tsc clean. Branch now 9 commits adce8c6..43d7fb7, still READY TO MERGE pending user manual checklist.)
WAVE-D MERGED to main 2026-07-08 (merge 39644e1, branch deleted). User manual checklist PASSED (incl. AM/PM display). Post-merge tests+tsc green. PHASE D COMPLETE. Phase E (Tasks 19-23) next.

WAVE-E (branch wave-e-owner-medium, base 1562d61, plan 2026-07-08-wave3-map-combined.md Phase E Tasks 19-23):
Task 19: complete (commit 17034f4, lib/customerLookup filter + CustomerLookup combobox + 3 drawer swaps [LeadDrawer/JobDrawer hidden-input, InvoiceDrawer controlled onPick] + leads/jobs/map pages widened to id,name,phone,address + lookup CSS; 152/152 unit, lint+tsc+build clean, review approved, no fixes. Minor deferred to final review: initial find() unmemoized on hot path, aria-controls id absent while closed [same pattern as MapSearch, plan-mandated])
Task 20: complete (commits ffb8540+5016fec, migration 0019 customers.active + setCustomerActive admin action + drawer Deactivate/Reactivate + INACTIVE chip + ?inactive=1 admin toggle [tamper-proof] + 4 lookup feeds active-only; review found Critical invoice Bill-to blanked for deactivated customer [InvoiceDrawer re-derived from filtered array], fixed 5016fec at buildInvoices layer [contact fields from unfiltered join + picked??invoice-fallback, print payload covered] + InvoiceDrawer.render.test.tsx RED->GREEN, re-review approved; 96 pgTAP, 156/156 unit, lint+tsc+build clean. Minor deferred to final review: GlobalSearch surfaces inactive customers [defensible, follow-up ticket], 'Bill to—' textContent-concat assertion fragile [positive assertions carry weight])
Task 21: complete (commit 672dec0, migration 0020 deleted_at + leads_public/jobs_public recreates [OR-chain parenthesized correctly] + soft delete/restore RPCs + 5-RPC sweep + admin query sweep + ?deleted=1 History UI w/ Restore; opus reviewer diffed all 10 recreated bodies vs sources line-by-line — faithful; implementer caught brief's STALE provenance hint [claim_job latest is 0016 SEC-1 bigint narrowing, not 0009 — 0009 copy would reintroduce price leak]; pgTAP 116/116, 156/156 unit, lint+tsc+build+db reset clean, review approved, no fixes. Minor deferred to final review: leadDetail/createInvoiceFromJob single-row fetches unfiltered [admin-only origin info of deleted lead], claim_job on deleted raises generic 'already claimed' msg, Restore uses useTransition pattern not literal form [disclosed, accepted])
Task 22: complete (commits ebff011+51172f4, migration 0021 rep_id + backfill created_by + leads_public recreate [rep_id added, deleted_at guard kept] + create_lead/update_lead p_rep_id [create: coalesce auth.uid(), update: coalesce keep] + 0016 SEC-4-narrowed grant list extended [implementer caught brief's stale 0015 hint — 0015 copy would reintroduce spoofing vector] + LeadDrawer Rep select/display + leads page reps fetch; review found Important map LeadDrawer edit path empty Rep dropdown [controller dispatch context wrongly called path read-only], fixed 51172f4 [map widens existing profiles fetch, threads reps/uid], re-review approved; pgTAP 119/119, 162/162 unit, lint+tsc+build+db reset clean. Minor deferred to final review: reps filter duplicated leads/map pages [repOptions helper], rep can PATCH rep_id via PostgREST outside RPC [plan-mandated, note for Tier-3 commission model])
Task 23 Step 1 battery (controller-run 2026-07-08, pre-fix HEAD 51172f4): lint clean, tsc clean, 162/162 unit (24 files), pgTAP 119/119 (8 files), supabase db reset 0001-0021+seed clean, build all routes — ALL GREEN.
WHOLE-BRANCH REVIEW (fable): WITH FIXES -> 261437c applied via single fixer (GlobalSearch .eq(active,true) [dead-end click], migration 0022 create_lead_from_pin rep_id=v_uid [Important — PLAN GAP, pin workflow bypassed commission attribution; controller fix-now call on owner-locked 'default=current user' semantics, FLAG TO OWNER for veto] + pgTAP via leads_public under rep JWT, dashboard select +rep_id [honest cast], LeadDrawer legacy rep (legacy) fallback option + render test [silent-reassignment bug]). Reviewer CONFIRMED all 4 line-by-line vs sources: READY TO MERGE. Post-fix battery green: lint+tsc+165/165 unit+pgTAP 120/120+build.
WAVE-E RIDERS (named, carried): CustomerLookup initial find() unmemoized + reps filter dup leads/map pages [repOptions helper] (lookup-polish); aria-controls absent-while-closed CustomerLookup+MapSearch (a11y, fix both together); 'Bill to—' textContent-concat assertion fragile (test polish); leadDetail/createInvoiceFromJob unfiltered deleted_at [admin-only, invoice path UI-unreachable] (doc); claim_job on soft-deleted raises generic 'already claimed' (wording, next claim_job touch); rep can PATCH rep_id via PostgREST [plan-mandated, revisit at Tier-3 money]; 0022 header comment tail garbled (cosmetic). Prior-wave carry backlog unchanged (see WAVE-D entry).
WAVE-E MERGED to main 2026-07-08 (merge a25d79f, branch deleted). Post-merge 165/165 + tsc green. PHASE E COMPLETE pending user manual acceptance walkthrough (Task 23 Step 2, batched below in session summary; 0022 pin rep_id fix flagged for owner veto). WAVE 3 REMEDIATION PLAN FULLY EXECUTED (Phases A-E, Tasks 1-23).
Walkthrough polish (owner feedback during Phase E checklist): commit 53e2dc6 on main — CopyButton border matches .qa anchors; HistoryToggle .chip->.btn sec + moved into right action group (KanbanBoard/JobsBoard/LeadsListSection/JobsListSection, mirrors customers inactive placement); history screens keep toggle+Export+New trio, Export scoped to deleted rows (leads/jobsHistoryCsvTable + Deleted col, filenames *-history.csv), toggle label '← Back to …' when active, New navigates out to ?new=1. 167/167 unit, lint+tsc+build clean.
User manual Phase D/E acceptance walkthrough PASSED 2026-07-08 (incl. polish 53e2dc6; 0022 pin-rep fix not vetoed). WAVE 3 CLOSED. Tier-3 money-model discussion starting.

MONEY MODEL (branch feat/money-model, base 94cc3cc, plan 2026-07-08-money-model.md):
Task 1: complete (commit c06cd66, migration 0023 schema+RLS job pot/members/expenses/profiles_private/jobs_rep; pgTAP 132/132 [12 new], 167/167 unit, lint+tsc clean, review approved, no fixes. Minor deferred to final review: insert-denied checks exercise rep role only [grant role-independent], jobs_rep seam comment thin)
Task 2: complete (commit 52f9d7e, migration 0024 join/expense RPCs + payout-on-done + earnings/revenue views; provenance sweep found brief hints stale — claim_job/update_job/set_job_status/jobs_public newest in 0020, create_job in 0018, 0021/0023 prose-only traps avoided; opus reviewer verified all 5 recreated bodies line-by-line faithful, mandated deltas only; "scheduled" bounce implemented as in_progress [enum has no scheduled — plan-text error]; pgTAP 198/198 [+66], 167/167 unit, lint+tsc clean, review approved, no fixes. Minor deferred to final review: company_revenue net assertion tautological [no independent expected figures], done-bounce overwrites original done_at timestamp [confirm with owner])
Task 3: complete (commit 78bc0de, lib/jobs Job+JobMember/buildMembers/shareOf/parseJobForm cleaner_amount + lib/earnings monthKey/leaderboard + expensesCsvTable + requestJoin/decideJoin/addExpense/deleteExpense actions + p_cleaner_amount threading; 2 extra test files touched for Job type widening [disclosed, verified behavior-neutral]; 179/179 unit, lint+tsc clean, review approved, no fixes. Minor deferred to final review: shareOf(0,2) + leaderboard([]) untested, addExpense job_id NaN guard absent [brief-inherited; CORRECTED per final review: NaN serializes to null → expense saved silently UNLINKED, not rejected — near-unreachable from type=number UI], monthKey string-slice UTC coupling [same dayTime convention, known backlog])
Task 4: complete (commits ac39db3+3d88e75, JobDrawer members panel/join/pot input + JobCard pending badge [admin+owner only] + rep data branch BOTH pages + SPEC-GOVERNS FIX: migration 0025 create_job/update_job guards admin→admin/rep [spec "rep see/set both" + plan Task 8 walkthrough "rep creates job"; crud_rpcs pins flipped, rep delete/history/invoice stay admin] + money display flag through board/list/CSV + rep Edit access; pgTAP 200/200, 184/184 unit, lint+tsc+build clean, review approved, no fixes. Reviewer verified 0025 bodies byte-identical to 0024 minus guard. Minor deferred to final review: JobsBoard canCreate re-derives vs JobsListSection reuses money prop [DRY drift risk], no render coverage for pendingCount badge gating, money flag overloaded view-price vs write-jobs [same role set today]. FLAG TO OWNER at walkthrough: rep job create/edit widened per spec — veto point.)
Task 5: complete (commits f401d2c+029c60a, /expenses page month-grouped table + add/delete + CSV + nav 07/Settings 08; review found Important add-form no-reset-after-success, fixed 029c60a [formRef.reset() on success, UsersPanel pattern] + render test, re-review approved. Disclosed deviation accepted: React 19 form-action auto-resets uncontrolled inputs, error-path value retention not assertable — error test pins role=alert instead. 188/188 unit, lint+tsc+build clean. Minor deferred to final review: ExpensesSection.tsx:95 comment claims error keeps values [contradicts React 19 reality, one-line fix], no delete confirm [owner call], job_id===0 falsy at :138.)
Task 6: complete (commit b445b04, dashboard MoneyRow [month rev/exp/net + all-time net + →Expenses link] + transparent Leaderboard [month⇄all toggle, aria-pressed, own-row bold] + cleaner My-earnings card; company_revenue/cleaner_earnings fetched unconditionally [view self-gates]; 192/192 unit, lint+tsc+build clean, review approved, no fixes. Disclosed judgment accepted: cleaner card jobsDone all-time scope [brief ambiguous]. Minor deferred: revenueRows type says number but PostgREST may send strings [Number() casts defend at use sites], own-row bold whole tr vs name cell [wording deviation only].)
Task 7: complete (commit ebbaee8, create-user phone/DOB → profiles_private [service-role client, same as profiles insert, non-fatal logQueryError, skip when both empty] + tel/date inputs w/ autofill guards + users table Phone/DOB cols em-dash null + settings page fetch/Map-join; no edit flow existed, none built [per brief — owner edits via re-create or SQL until later pass]; 194/194 unit, lint+tsc+build clean, RLS spot-check curl [cleaner own-row ok, colleague blocked], review approved, no fixes. Minor deferred: no server-side phone/dob format validation [bad dob just fails insert non-fatally], actions.ts skip-branch untested [needs supabaseAdmin mock].)
SESSION END 2026-07-09 (session 1): Tasks 1-3 committed on feat/money-model (c06cd66, 52f9d7e, 78bc0de), NOT merged. Battery green at that point: 179/179 unit, pgTAP 198/198.
Task 8: complete (verification pass 2026-07-09 session 2). Step 1 battery green pre-review: lint, tsc, 194/194 unit (29 files), build, db reset 0001-0025, pgTAP 200/200.
WHOLE-BRANCH REVIEW (fable): initial verdict NO — 1 Critical + 2 Important, all PLAN/SPEC DEFECTS faithfully implemented, not implementation errors: (C1) join-request flow unreachable — cleaners couldn't see foreign claimed jobs anywhere (jobs_public WHERE + visibleJobs), so Request-to-join undiscoverable AND approved non-owner members couldn't see joined jobs; spec self-contradiction (line 74 vs 79). OWNER DECIDED 2026-07-09: cleaners see ALL non-deleted jobs incl. done; join while not done; deleted/restore stay admin-only. (I2) admin unclaim left stale approved members → wrong future split + orphaned can_decide_join power, no remediation path. (I3) soft-deleting a done job kept its auto-payout expense while excluding its revenue → net understated, books/leaderboard inconsistent.
FIXES a8224c3 (migration 0026: jobs_public WHERE→deleted_at is null; set_job_status unclaimed branch deletes job_members; company_revenue exp CTE excludes payouts of soft-deleted jobs [self-heals on restore, manual expenses stay]; decide_join raises on deleted job; visibleJobs→identity seam + test flips; rls_money assertion 9 fixture-scoped [old global count asserted old policy]; pgTAP money_model 78→102) + 68f80ec (expense delete window.confirm + accept/decline tests; false React-19 comment fixed). Reviewer CONFIRMED all resolved, deviations sound (fixer's standalone decide_join guard strictly better than reviewer's suggested in-place edit): READY TO MERGE.
Post-fix battery green (controller-run): lint, tsc, 195/195 unit, build, db reset 0001-0026, pgTAP 224/224 (9 files). Branch feat/money-model 11 commits ac39db3..68f80ec + ledger, NOT MERGED — owner acceptance walkthrough (Task 8 Step 2) pending.
CARRY backlog (final-review triage): canWriteJobs rename fold [JobsBoard canCreate re-derive + money flag overload], pendingCount badge render coverage, job_id===0 falsy, revenueRows string-numeric type nit, own-row bold whole-tr, phone/dob server validation + createUser silent phone/dob drop notice [pair together], skip-branch test, rep-only insert-denied pgTAP, done-bounce overwrites done_at [owner confirm at walkthrough], shareOf(0,2)/leaderboard([]) untested, addExpense NaN→unlinked, monthKey UTC coupling, jobs_rep policy lacks deleted_at guard [rep reads soft-deleted price via PostgREST — same family as rep PATCH carry], dashboard jobsThisWeek for cleaners now counts colleagues' jobs [consistent w/ owner decision, note at walkthrough], two id=900003 rls_money assertions slightly redundant.
WALKTHROUGH BUG FIX (user-reported, 2026-07-09): "this.errorCb is not a function" on /map — mapbox-gl telemetry crash: remove() nulls errorCb, late-failing telemetry POST still calls it; StrictMode dev double-mount guaranteed a removed throwaway Map every visit (needs failing events.mapbox.com POST to fire — likely adblock/DNS on owner's box). No upstream fix (3.26.0 changelog checked). Fixed e92e733: MapboxMap defers construction one cancelled-on-cleanup rAF (throwaway mount builds nothing), map moved to state so marker/flyTo effects re-sync on landing; TDD'd (RED: 2 Maps under StrictMode; GREEN: 1) in new MapboxMap.render.test.tsx. 198/198 unit, lint+tsc+build clean. Not a money-model regression — pre-existing, dev-amplified.
WALKTHROUGH UX INVESTIGATION (user-reported "loading on every screen", 2026-07-09): two opus investigations (static diff audit + A/B timing vs main worktree, same DB, cookie-authed medians). Findings: NO perf regression — layout/proxy/auth 0-line diffs, all new queries parallel, warm SSR flat vs main except /dashboard +27-36ms (two new report views, app-code only); framework baseline identical. Root cause of perception: app/(app)/loading.tsx (Wave-A Task 5, 93a324c, merged main 2026-07-08) swaps EVERY navigation to skeleton, surfacing the always-present ~200ms render; pre-Wave-A default kept previous page visible. OWNER DECIDED: deactivate globally, keep file. Commits 81f1dbe+df9cdb8: moved to components/shell/RouteLoadingSkeleton.tsx (convention path is the activator; null fallback would blank-flash) w/ per-route re-enable recipe in header. error.tsx untouched. Battery green: 198/198, lint+tsc+build. NOTE: partially reverses Wave-A Task 5 by owner call — dashboard +30ms is the only real (sub-perceptual) cost of the money branch; auth round-trip consolidation backlog remains the lever if SSR time ever matters.
WALKTHROUGH FEEDBACK ROUND 1 (owner, 2026-07-09 — EXECUTE NEXT SESSION, branch still unmerged; owner mandate: ALL frontend design via ui-ux-pro-max skill + strict cross-screen consistency):
(1) Request-to-join button: bigger, above Close in actions row, bordered, red/in-progress color initially; after request → gray "Requested" button. (2) JobCard pending badge: amber chip ugly/border misfit — redesign. (3) Joiners UI: move from under-pot to table below actions/Close area; empty state "no joiners requested" (or hide). (4) Expenses: create → "New expense" button + side drawer [consistency]; job-id field → autocomplete showing customer+address; delete button → .btn-danger red consistent. [Grouping explained to owner: monthly headers+subtotals, not daily.] (5) BUG/UX: drag unclaimed→claimed blocked (claim path needs assignee) — owner Q pending on desired semantics. (6) Pot: show under Job Details beneath Price; keep "your share"; drop per-member share numbers. (7) JobCard: show job number in board view. (8) BUG: InvoiceDrawer Bill-to preselects first customer on create — start empty. (9) Create user (and expense) = button + side panel closing on success, like lead/job. (10) Cleaners tab/board: nav tab — cleaner name/jobs/total earned, no revenue (dashboard leaderboard relationship Q pending). (11) Jobs nav red badge = unclaimed count; owner wrote jobs tab visible admin+cleaners only — CONFLICTS w/ 0025 rep job-write, Q pending. (12) Recurring jobs — needs brainstorm+spec (cadence/spawn/copy semantics). (13) "Done→expense not working" DIAGNOSED NOT A BUG: owner's job had NULL pot (DB checked live: done job 3 pot NULL; pot-150 job never done; payout+bounce pgTAP-proven). Follow-ups: warn on Done w/o pot, revalidatePath('/expenses') in setJobStatus, 3 near-dup manual expenses observed (possible owner testing vs double-submit gap). Full list also in memory money-walkthrough-feedback-2026-07-09. OWNER ANSWERS: #5 drag unclaimed→claimed = claim-to-self for cleaner AND admin (admin does work too; rep drag stays blocked); #11 reps KEEP jobs tab, red unclaimed badge admin+cleaner only; #3 ONE table all members (★ owner/approved/pending w/ actions, empty state), replaces under-pot list; #10 cleaners tab all roles, dashboard keeps compact leaderboard linking to it (admin earning shares via claims = accepted).
OWNER VETO POINTS AT WALKTHROUGH: (1) rep can now create/edit jobs incl. money (0025, spec-mandated); (2) cleaners see all jobs interpretation — deleted jobs stay hidden from cleaners ("cannot restore" read as restore-stays-admin, NOT cleaner-visible history); (3) done-bounce resets done_at timestamp.

POLISH WAVE (plan 2026-07-09-money-polish-recurring.md, branch feat/money-model, WAVE-BASE SHA 70e839a — Task 10 whole-branch review diffs from here): pre-wave battery green 2026-07-09 (lint, tsc, 198/198 unit, db reset 0001-0026, pgTAP 224/224).
Task 1: complete (commit f1d5a40, migration 0027 recurring jobs — recur_days/recur_parent_id + once-only partial unique index + create_job/update_job p_recur_days + set_job_status spawn-on-done; provenance verified 0025/0026 line-by-line by opus reviewer; pgTAP 262/262 [+38, plan(140)], 198/198 unit, lint+tsc clean, review approved, no findings. Minor noted: 0-coerced-to-NULL relies solely on case-expression [check constraint would reject raw 0] — safe today.)
Task 2: complete (commit dfe6e28, Job recur fields + parseJobForm recur_days [blank→0=clear] + canTransition unclaimed→claimed admin/cleaner + p_recur_days threading + revalidatePath('/expenses') in setJobStatus + admin/rep selects widened both pages [jobs_public untouched]; 205/205 unit, lint+tsc clean, review approved. 4 extra test files mechanically widened — reviewer verified additive-only. Important sequencing note [controller-resolved]: edits wipe recur_days until Task 3 drawer input lands — branch unmerged, Task 3 next, no exposure window.)
Task 3: complete (commits 66959f3+5c9726f, JobDrawer polish — Cleaner-pot kv under Price, members table .tblwrap/.tbl below actions [★ owner, Approve/Reject canDecide-gated, empty state], Request-to-join --prog accent above Close w/ gray Requested state, recur kv rows admin/rep + edit-form recur_days input [closes Task 2 wipe gap], per-member shares removed, zero new CSS; review found Important test-coverage regression [5 cases replaced not extended — rep-Price + approved-member-no-join assertions deleted], fixed 5c9726f stronger-than-original, re-review approved; JobDrawer file 6/6, suite 206/206, lint+tsc clean. Minor deferred to final review: Spawned-from #NNNN row + recur defaultValue round-trip untested, case (a) three-concern test.)
Task 4: complete (commit baf4790, JobCard #0042 meta number + .pendchip pill [--follow bg, --on-status text, replaces amber .lbl, gating unchanged] + drag unclaimed→claimed routes claimJob [pending-gate/error surfacing preserved] + done-without-pot confirm both paths [byte-identical message, null/0 prompt, >0 no prompt, decline aborts]; 215/215 unit [+9], lint+tsc clean, review approved after controller adjudication: reviewer's Important [.pendchip min-height omitted] resolved against spec §B.4 — fitted display-only pill, 44px rule scoped to interactive touch targets, no fix needed. Minor deferred: .lbl-absence assertion scope, additive pill polish props.)
CHUNK 1 (Tasks 1-4, recurring jobs core) COMPLETE 2026-07-09. Controller battery green at baf4790: lint, tsc, 215/215 unit (31 files), db reset 0001-0027 + seed, pgTAP 262/262 (9 files). Branch feat/money-model, wave commits f1d5a40..baf4790 (5 commits incl. Task 3 test fix). Next session: Tasks 5-9, then Task 10 (whole-branch review diffs from WAVE-BASE 70e839a + owner walkthrough round 2). Do NOT merge — owner decision.
Task 5: complete (commit 6e6cc83, expenses create drawer + JobLookup combobox [exact prop contract, CustomerLookup ARIA/keyboard mirror] + btn-s btn-danger delete; jobOptions Promise.all fetch w/ padStart-4 em-dash labels; 218/218 unit, lint+tsc clean, review approved. Minor deferred to final review: customers fetch lacks deleted_at filter [soft-deleted customer still labels job]; JobLookup uses bare .searchbox vs CustomerLookup two-line variant [judgment call].)
Task 6: complete (commit 7c96bee, create-user side drawer matching Task 5 pattern [creating state, scrhead button, Drawer labelId, close+reset on success only, role=alert keeps open]; fields/autofill guards byte-preserved, .userform grid 7->6 cols; 4 new render tests, 222/222 unit, lint+tsc clean, review approved, no findings beyond cosmetic notes.)
Task 7: complete (commit f5c9cf1, /cleaners route all roles [layout guard covers auth, no revenue anywhere] + nav 07/renumber 08-09 + TITLES + Leaderboard limit/moreHref props [default byte-identical] + dashboard limit=5 moreHref; zero-row append both boards; 227/227 unit, lint+tsc+build clean, review approved. Minor deferred: uid ?? '' unreachable fallback; zero-row-append inline logic untested [codebase convention].)
Task 8: complete (commit 671a195, unclaimed-jobs .navbadge on Jobs nav item [--lost bg, 999px radius, min-width 18px] via NavLink [deviation from brief's MobileNav — MobileNav is pure Drawer wrapper, mobile gets badge through Topbar->Sidebar chain, reviewer verified component tree]; layout head-count jobs_public admin/cleaner, rep null, logQueryError->null; 5 new render tests, 232/232 unit, lint+tsc clean, review approved. Minor deferred to final review: .navbadge literal #fff on dark-theme --lost ~2.8:1 contrast [plan said "white text"; consider --on-status token like .pendchip] — a11y follow-up candidate.)
Task 9: complete (commit e3a9c34, InvoiceDrawer Bill-to empty on create [useState invoice?.customer_id ?? 0, no customers[0] fallback] + 'Pick a customer' guard before startTransition reusing form-err slot; picked ?? invoice chain untouched [edit/print verified]; 234/234 unit, lint+tsc clean, review approved, no findings.)
CHUNK 2 (Tasks 5-9, create-flow consistency + cleaners tab + badge + bill-to fix) COMPLETE 2026-07-09. Controller stop-point battery green at e3a9c34: lint, tsc, 234/234 unit (33 files), npm run build, db reset 0001-0027 + seed, pgTAP 262/262 (9 files). Chunk commits 6e6cc83..e3a9c34 (5 commits, one per task). Next session: Task 10 ONLY — full battery, whole-branch final review diffing from WAVE-BASE 70e839a (all Minors deferred above feed its triage), owner walkthrough round 2 checklist. Do NOT merge — owner decision.
MERGED TO MAIN 2026-07-09 (user directive, merge commit 9562cf2, pushed to origin; feat/money-model deleted local+remote). Post-merge battery green on main: lint, tsc, 234/234 unit, build, db reset 0001-0027, pgTAP 262/262. NOTE: Task 10 whole-branch final review + owner walkthrough round 2 SKIPPED by merge order — deferred Minors from chunks 1-2 (ledger above) remain open follow-ups; owner veto points (rep job-write 0025, deleted-jobs visibility, done-bounce done_at reset, navbadge contrast) now live on main unreviewed.
Task 10: complete (2026-07-09, EXECUTED POST-MERGE on user order — review ran against main since feat/money-model deleted; fixes on branch fix/money-polish-final-review off main 9a66c30, NOT merged). Step 1 battery green pre-review on main: lint, tsc, 234/234 unit, build, db reset 0001-0027, pgTAP 262/262.
WHOLE-WAVE FINAL REVIEW (fable, 70e839a..9a66c30): verdict FIXES NEEDED — 1 Critical: drawer "Claimed" statuspick on unclaimed job bypassed claim_job (Task 2 enabled the transition, only the board got claim-routing; admin path made ownerless claimed jobs → done inserts payout with zero earnings recipients, cleaner path errored "not yours"); 2 Important: UsersPanel role-change error invisible (sole role=alert moved inside creating&&Drawer), pot row gated on claimed_by (spec §B1 — cleaner claimed blind); Minors: /cleaners month board dropped past-month-only earners (spec-governs over plan text), pgTAP bounce count=1 couldn't distinguish survived vs delete+respawn, JobLookup Enter preventDefault (note-only, CustomerLookup mirror). Verbatim-copy discipline, spawn safety (no forgery path — recur_parent_id never a param), and visibility matrix all verified CLEAN.
FIXES (single fixer, branch fix/money-polish-final-review, 5 commits 6360306..980d74b): 6360306 change() short-circuits unclaimed→claimed to claim() before done-confirm gate + pot row un-gated (Your-share gate kept) + T3 tests (Spawned-from #0123, recur defaultValue round-trip via Edit click) + lib/jobs comment both call sites; 730d24e separate roleError role=alert above users table (drawer path byte-identical); ed1a6aa .navbadge #fff→var(--on-status) (T8); 938c72d zeroRowsFor(board) per-dataset; 980d74b pgTAP successor same-id pinned between bounce and re-done + hasnt_column jobs_public recur_parent_id, plan(142). Reviewer re-verified line-by-line: CONFIRMED RESOLVED, no new issues.
DEFERRED-MINOR TRIAGE (final): FIX-NOW T3+T8 done in fix batch; CARRY: T1 recur 0-coerce case-expr (RPCs sole write path, constraint is backstop), T4 .lbl-absence assertion scope, T5 customers deleted_at filter on expense job labels (soft-deleted name arguably better than em-dash; owner call if surfaces) + JobLookup bare .searchbox, T7 uid??'' dead fallback + zero-row-append untested. Prior money-model carry backlog (Task 8 entry) unchanged.
Post-fix battery green (controller-run on fix branch): lint 0, tsc clean, 241/241 unit (33 files), build all routes, db reset 0001-0027 + seed, pgTAP 264/264 (9 files). POLISH WAVE Task 10 COMPLETE. AWAITING: owner walkthrough round 2 (checklist handed) + owner merge decision on fix/money-polish-final-review. Reviewer flagged for walkthrough: drawer Claimed chip on unclaimed job, pot visible on unclaimed job, failed role change on /settings.
WALKTHROUGH BUG (owner, 2026-07-09, navbadge hydration mismatch on /dashboard): systematic-debugging root cause = STALE SERVICE WORKER, not app code. Owner's browser held /sw.js (registered during an earlier prod-mode PWA session — SWRegister's dev gate only skips NEW registrations); it serves /_next/static/ cache-first with no revalidation, and Turbopack DEV chunk URLs are path-derived NOT content-hashed (proven: content edit, URL list byte-identical) → after code moved on, normal reload = fresh HTML/flight + stale pre-badge NavLink chunk → client tree missing .navbadge, the exact reported diff. Fingerprints: incognito clean (no SW), ad-blocker off no effect, hard-clear works once (force-reload bypasses SW) then normal refresh breaks again (cache-first never revalidates). Reproduced end-to-end in Playwright (prime SW cache with pre-badge NavLink, restore code, normal reload → hydration error + badge gone after recovery); clean-browser scenarios (plain/count-change/back-nav/SW-with-current-code) all zero recoverable errors — NOTE: hydration errors surface via onRecoverableError/reportError, NOT console.error (instrumentation trap, cost one false pass). FIX d5c4192 (TDD, +2 tests, 243/243): SWRegister non-prod branch actively unregisters all registrations + drops all CacheStorage caches; sw.js CACHE v2→v3 + install skipWaiting + activate clients.claim (byte-diff forces worker update via updateViaCache:'none' — required escape hatch because the stale cache also held the OLD SWRegister chunk, so the eviction code alone could never reach affected browsers). Heal path validated in Playwright (SW gone, caches dropped, 0 errors across 3 reloads). Battery green: lint, tsc, 243/243 unit (34 files), build, db reset (also restored clean seed after probe claims), pgTAP 264/264. Branch now 7 commits 6360306..d5c4192.
OWNER WALKTHROUGH ROUND 2 PASSED + MERGED TO MAIN 2026-07-09 (owner order "everything is done, merge and clean": merge 83bbe46, pushed to origin; fix/money-polish-final-review deleted). Post-merge battery green on main: lint 0, tsc clean, 243/243 unit (34 files), build all routes, db reset 0001-0027 + seed, pgTAP 264/264. POLISH WAVE FULLY CLOSED (Tasks 1-10 + final-review fixes + stale-SW fix). Owner veto points now walkthrough-accepted (rep job-write 0025, deleted-jobs cleaner visibility, done-bounce done_at reset). Open backlog for next sessions: Task 10 CARRY triage (T1 recur 0-coerce, T4 .lbl assertion scope, T5 expenses customers deleted_at filter + JobLookup searchbox variant, T7 uid fallback + zero-row-append test, JobLookup Enter note) + money-model Task 8 carry list. NEXT per owner: "big fixes" — new session.
POST-WALKTHROUGH UI FIXES (owner, 2026-07-09, commit 0196a84 on main): (1) Approve/Reject rendered as bare text — root cause: .btn-p/.btn-s got padding/typography only from `.acts button`, none standalone; added base rule (matches .btn) + hover + coarse-pointer 44px list membership for .btn-p/.btn-s/.btn-danger. (2) Create-user drawer fields side-by-side — .userform was 6-col grid stacking only ≤900px, drawer narrow always → 1fr single column + full-width inputs + visible labels on full name/email/password/role (Phone/DOB pattern). (3) "Delete expense missing" — NOT a regression (git-verified always present); unstyled-text look + manual-rows-only gating (job_payout shows em-dash by design) explained it; styling fix resolves. Battery green: lint 0, tsc clean, 243/243 unit. Owner's "big fixes" list still unknown beyond these three.

MAP DOTS (branch feat/map-dots, WAVE-BASE 880ef01, plan 2026-07-14-map-dots.md, phases 2026-07-14-map-dots-phases.md — Phase D whole-branch review diffs from WAVE-BASE):
PHASE A (Tasks 1-3, DB layer) COMPLETE 2026-07-14. Pre-flight baseline 9 files/264 pgTAP PASS (matched ledger).
Task 1: complete (commit f873fa8, migration 0028 dots enum+table+read RLS [select policy auth.uid() not null + select grant ONLY, no write grants/policies] + create/update/delete_dot RPCs [NULL-safe coalesce role check, delete idempotent, update raises not-found]; verbatim-to-plan transcription verified by reviewer; dots.sql 17/17, battery 10 files/281; review approved, no fixes. Minor noted: no dots(lat,lng) index [no viewport queries planned — revisit if dot volume grows].)
Task 2: complete (commit ea94a10, convert_dot_to_lead/job appended to 0028 — claiming DELETE...RETURNING [no read-then-delete, coords from dot row only], provenance verified line-by-line by opus reviewer [customers insert mirrors 0022 create_lead_from_pin + brief-mandated phone col; leads insert 0021 column set w/ quote coalesce; jobs insert mirrors 0027 create_job price-coalesce-0/unclaimed/no-recur; 0027 confirmed newest create_job], won->job trigger 0006 fires on lead convert, job convert no lead row; dots.sql 34/34, battery 298. Disclosed deviation adjudicated OK: 3 numeric->bigint::text casts in test read-backs formatting-only [whole-number literal inputs, no truncation risk]. Minor deferred: trim_scale() would be tighter than ::bigint cast [dots.sql:158,190,205].)
Task 3: complete (commits 701d761+1ae9941, migration 0029 leads_rep select policy [rep AND deleted_at is null — soft-deleted stays admin-only] + create_lead/update_lead v_admin->v_money; provenance verified by opus reviewer [0021 confirmed newest: create_lead 0014->0021, update_lead 0014->0020->0021; bodies byte-identical except gate]; NO write-grant changes [quote_value stays out of INSERT/UPDATE column grants — direct-PATCH protection intact]; rep_quote.sql 7/7. Disclosed deviations adjudicated OK: (a) ::bigint::text casts tests 2/4 formatting-only; (b) crud_rpcs.sql test 25 expectation 0->777 [old test pinned pre-0029 rep-quote-coerced-to-0; 777 = the update_lead arg; no other file asserts old behavior — implementer left it uncommitted, controller caught + committed 1ae9941]. Reviewer notes non-blocking: 0029 jobs_rep-parity comment slightly imprecise [0023 jobs_rep lacks deleted_at scope; leads_rep divergence intentional].)
PHASE A GATE green 2026-07-14 (controller-run at 1ae9941): npx supabase db reset 0001-0029+seed clean, pgTAP 11 files/305 PASS (dots 34, rep_quote 7, crud_rpcs 34), lint 0, tsc clean, 243/243 unit (34 files), build all routes. Branch feat/map-dots 4 commits f873fa8..1ae9941, NOT merged, NOT pushed. Deferred minors for Phase D triage: dots(lat,lng) index absent, ::bigint vs trim_scale cast tightness, 0029 parity comment wording. Phase B next: Tasks 4-6 (lib vocab/parsers -> server actions -> popup components).
Task 4: complete (commit 8d0c618, lib/dots.ts vocab/colors/Dot type/convert parsers + lib/mapPins.ts dot kind [dots param default [], visibleMapPins 3-way filter]; DotStatus verified byte-for-byte vs 0028 enum, colors spec-locked, page.tsx 3-arg caller unaffected; 253/253 unit, tsc clean, review approved, no findings)
Task 5: complete (commit 7730ef7, 5 dot actions appended to map/actions.ts [createDot/updateDot/deleteDot/convertDotToLead/convertDotToJob], revalidateDots helper /map+/dashboard + convert extras /leads|/jobs+/customers, redirect unguarded; reviewer cross-checked all RPC names/params vs 0028 verbatim, createLeadFromPin untouched; tsc+lint clean, review approved, no findings. Reviewer note [pre-existing]: supabase client untyped — no compile-time RPC name safety codebase-wide)
Task 6: complete (commit 2778e78, DotPopover 3-view [main chips-save-immediately/lead/job forms, cleaner read-only zero-input] + DotCounts pill + 7 render tests; field names byte-match Task 4 parsers, "Cleaners Pay" label, form-err role=alert all views; review approved, no Critical/Important. Notes for Task 7 reviewer: .pop-dot/.dotcounts CSS intentionally absent until Task 7 [confirm .dotcounts i gets sized there]; chip-click-with-EDITED-fields untested [correct by construction]; spec prose "legacy service option" N/A on create-only forms)
PHASE B GATE green 2026-07-14 (controller-run at 2778e78): 260/260 unit (36 files), tsc clean, lint 0, build all routes. Map page still on OLD PinPopover flow by design — integration is Phase C. Branch feat/map-dots commits f873fa8..2778e78 (7), NOT merged, NOT pushed. Phase C next: Tasks 7-9 (map integration + rep quote UI + MiniMap).
Task 7: complete (commit dea0367, map integration: MapImplProps.onPinClick widened to (pin,xPct,yPct) across SchematicMap/MapboxMap/MiniMap[arity-only], MapView reworked [dot popup state machine with BOTH close rules as render-phase adjustments — drawer-key change + absence-from-props with fresh exemption; Dots toggle chip; DotCounts showDots?dots:[]; hint copy], map page dots fetch, .mpin-dot/.pop-dot/.dotcounts CSS, nav hint updated; PinPopover/createLeadFromPin/parsePinForm/PinInput deleted — repo-wide grep ZERO hits, DB create_lead_from_pin untouched; review approved, spec compliant. Disclosed deviations accepted by reviewer: SchematicMap div-by-zero rect guard [real bug, jsdom 0x0 rect -> Infinity -> CSSOM crash], SchematicMap.render.test onPinClick assertion strengthened for new arity, 2 stale comments reworded for zero-hit grep. Minor deferred: MiniMap _x/_y unused-var lint warnings [cleared by Task 9])
Task 8: complete (commit abc40bd, LeadDrawer money prop split from admin [money gates quote view line~419 + quote input ~442 ONLY; Delete stays admin ~162]; leads page quote fetch+quoteById unconditional [page admin/rep-only], history fetch/section untouched; map page gates admin->canReadMoney, LeadDrawer money={canReadMoney}; admin->money rename cascaded one level deeper than brief's literal list — KanbanColumn/LeadCard/LeadsListTable — reviewer judged spec-MANDATED ["quote column/field" language], scoping verified [HistoryToggle stays admin]; 3 new LeadDrawer tests + 3 pre-existing updated money={true}; review approved. Minor deferred: no dedicated LeadCard/LeadsListTable money-gate unit test [pre-existing gap])
Task 9: complete (commit ca3343a, MiniMap prop Pin[]->prebuilt MapPin[], dot clicks route plain /map [id-collision guard, test covers same-id lead+dot], lead clicks keep /map?l=; dashboard fetches dots as 9th Promise.all entry [single query] + builds 3-kind pins; 1-arg onPinClick satisfies MapImplProps structurally — Task 7's 2 lint warnings cleared; review approved. DEVIATION for owner: plan's dashboard snippet EXCLUDES lost leads from MiniMap — old code only filtered null-geo, no lost filter existed; plan's literal reading applied [consistent with /map buildMapPins which skips lost]. Minor noted: dashboard dots unfiltered by design [spec: everyone sees all dots])
PHASE C GATE green 2026-07-14 (controller-run at ca3343a): 267/267 unit (37 files), tsc clean, lint 0 (MiniMap warnings gone), build all routes, npx supabase db reset 0001-0029+seed clean, pgTAP 11 files/305 PASS (dots 34, rep_quote 7). Branch feat/map-dots commits f873fa8..ca3343a (10), NOT merged, NOT pushed. Phase D next: Task 10 closeout (full battery, whole-branch review vs WAVE-BASE 880ef01, deferred-minor triage, owner walkthrough checklist).
PHASE D (Task 10, closeout) COMPLETE 2026-07-14. Pre-review battery green at 8bcd210 (lint 0, tsc clean, 267/267 unit, build, db reset 0001-0029+seed, pgTAP 11 files/305).
Whole-branch review 880ef01..8bcd210 (opus reviewer, spec section-by-section): all 7 focus areas PASS (convert atomicity claiming-DELETE sole read + provenance vs 0022/0021/0027; RLS/grant deltas exact — 0028 select-only, 0029 policy-only, quote_value out of column grants; popup state machine both close rules render-phase w/ fresh exemption; PinPopover retirement zero grep hits + DB create_lead_from_pin intact; rep quote money-gate w/ Delete admin-only + history admin-only; MiniMap collision routing w/ colliding-id test; test quality real-behavior). Verdict "With fixes": 0 Critical, 1 Important (MapView swallowed createDot failures — no popup, no error, knock silently lost on flaky network), 5 new Minors.
Fixes (single fixer, commit 5fa824c, TDD, reviewer RE-VERIFY PASS): Important — createError state + form-err role=alert in maptools row, no popup on failure, clears on next click, test covers alert/no-popup/clear-on-retry; Minor#4 — fresh-dot placeholder shows real clicked coords (OpenDot gains lat/lng, was 0.0000°); Minor#3 — dead Pin type deleted from lib/leads.ts (zero refs).
Deferred-minor triage (reviewer-adjudicated): ALL CARRY — (a) dots(lat,lng) index [only access is full-table select; index = pure write overhead until a bounds query exists]; (b) ::bigint vs trim_scale casts [cosmetic, can't mask wrong values]; (c) 0029 comment [accurate as written — declares the deleted_at delta explicitly; real gap is PRE-EXISTING 0023 jobs_rep letting reps read soft-deleted jobs — separate decision]; (d) untyped supabase client [codebase-wide, own task]; (e) chip-click-w/-edited-fields test [correct by construction]; (f) LeadCard/ListTable money-gate test [pre-existing gap; LeadDrawer tests cover the semantics]; (g) dashboard dots unfiltered [spec owner decision]. New carries: reviewer Minor#5 stale dots.sql test-20 comment ["no base read until 0029" — 0029 is applied; comment-only]; Minor#6 MapboxMap pin-click lacks SchematicMap's zero-rect guard [unreachable in practice — mounted mapbox container has size].
OWNER VETO POINT (reviewer Minor#2): dashboard MiniMap now EXCLUDES lost leads (old code showed lost leads w/ geo; new build matches /map semantics per plan's literal snippet — plan's "preserves old behavior exactly" claim was wrong). Flagged on walkthrough checklist below.
PHASE D FINAL battery green at 5fa824c: lint 0, tsc clean, 268/268 unit (37 files), build all routes, db reset 0001-0029+seed clean, pgTAP 11 files/305 PASS (dots 34, rep_quote 7). Branch feat/map-dots f873fa8..5fa824c (11 feature commits + ledger commits), pushed to origin. NOT merged, NO PR. AWAITING owner walkthrough — do NOT merge.
OWNER WALKTHROUGH CHECKLIST (run on local stack, npx supabase start + npm run dev):
[ ] Admin: click map -> dot drops (unmarked/blue), popup opens at click point
[ ] Rep: same — drop dot works
[ ] Cleaner: sees dots on /map, click opens read-only popup (no inputs/buttons besides close), map click does NOT create a dot
[ ] Status chips (Yes/No/Not Home/Callback/Unmarked): click recolors dot immediately + DotCounts pill updates
[ ] Dots layer toggle: hides/shows dots; counts pill zeroes when off
[ ] Convert -> Lead: form saves, LeadDrawer opens (/map?l=), dot GONE, lead visible on /leads, quote visible to rep
[ ] Convert -> Job: form saves ("Cleaners Pay" label), JobDrawer opens (/map?j=), price + cleaner pay stored
[ ] Convert -> Lead with status Won: job auto-spawned (won->job trigger)
[ ] Delete Dot: dot gone; second delete by teammate = silent success (no error)
[ ] Create-dot failure (e.g. offline): red error appears in map toolbar, no ghost popup
[ ] Dashboard MiniMap: dots render; dot click routes to /map (plain, no ?l=); lead click still opens drawer via /map?l=
[ ] VETO POINT: dashboard MiniMap no longer shows LOST leads (deliberate alignment w/ /map) — accept or veto
[ ] Rep on /leads: sees quote column/field, can edit quote, NO Delete button, NO deleted-leads history section
[ ] Admin on /leads: Delete button + deleted-leads history still present
OWNER WALKTHROUGH PASSED 2026-07-14. Veto point ACCEPTED (dashboard MiniMap lost-lead exclusion stays). Owner cleared merge. MAP DOTS CLOSED — merged to main (merge commit follows repo convention).
OWNER FEEDBACK CARRY (logged 2026-07-14, owner verbatim intent): does NOT like the dot popup (DotPopover) DESIGN — visual/UX gripe, no functional complaint, no specifics given yet. Backlog: redesign DotPopover look (get specifics from owner first; frontend-design rules apply — consistency #1). Candidate rider for small-changes wave or its own polish item.

DOTPOPOVER REDESIGN (branch feat/dotpopover-redesign, BASE d41a443, plan 2026-07-15-dotpopover-redesign.md, spec ...-design.md):
Task 1: complete (commit 2d42810, test contract red — dp-chip grid/--dp-c/data-lat-lng attrs + Unlabeled-dot fallback + MapView coords regression moved text->attrs; 3 new-contract assertions fail genuinely vs current component [reviewer statically verified], 13 others pass; review approved. Minor deferred: no-coords assertion appended at end of read-only test body instead of after 'Callback' line [cosmetic]. Reviewer ⚠️ resolved by controller: data-attrs-in-all-views not test-locked — plan-intentional, Task 2 code carries it, final review focus (c) covers.)
Task 2: complete (commit efe182c, DotPopover.tsx verbatim replacement per plan — dp- classes, --dp-c-only chip styling, data-lat/lng all views, minimal main top, read-only pop-dot+static chip+Unlabeled-dot fallback; reviewer verified handlers/state/transitions byte-identical via unchanged diff context lines + line-by-line brief transcription check; 16/16 contract, 269/269 full, tsc clean; review approved, zero findings.)
Task 3: complete (commit a9864f1, globals.css — .pop .row rules deleted [grep gate 0 hits, controller re-verified], pop-dot width 250->260, verbatim dp- block [6 color-mix all in-srgb, 12/35/70/26/14/75, caret ::after + dark border-color var(--line), dp-body scroll wrapper, dp-x inset hit area, no :focus-visible], coarse-pointer list + dp-chip/btn/save/danger; .statuspick/.pop .go/.pop .x untouched [reviewer verified in hunk context]; battery green 269 unit/lint 0/tsc/build; review approved, zero code findings. Step 6 manual visual check DEFERRED to owner walkthrough (autonomous run, no browser eyes).
Task 4: complete. Closeout battery green at a9864f1 (controller-run): 269/269 unit (37 files), lint 0, tsc clean, build all routes. No DB changes — pgTAP untouched by design.
WHOLE-BRANCH FINAL REVIEW (fable, d41a443..a9864f1, spec section-by-section): verdict READY FOR WALKTHROUGH. All 6 focus areas PASS: (a) handlers/state byte-identical [reviewer diffed old file region directly — review-diff hunks elided old lines 34-40]; (b) --dp-c sole inline chip style; (c) fresh-dot regression attrs '41.6730'/'-91.5480' exact-equality + coords spread on BOTH roots = all 4 views; (d) .statuspick survives + LeadDrawer:133/JobDrawer:241 consume it, .pop .go/.x survive; (e) 12/35/26/14/75/70 all in-srgb, reviewer computed WCAG: danger light ~5.7:1 [plain --lost would be ~4.0 — mix load-bearing], dark ~5.3:1, worst chip tint ~11:1; (f) coarse 44px covers all buttons, dp-x ::after = exactly 44x44.
NEW MINORS (all CARRY, reviewer-adjudicated): (1) dp-x 44px hit area unconditional — steals ~26x26px top-right corner of label input ON MOUSE too (mpin precedent scopes expansion to coarse); text/caret protected by padding-right 36px; cheap hardening = move .dp-x::after into coarse block — FLAGGED on walkthrough; (2) focus rings paint-clip at .dp-body edges (card padding outside scroll wrapper) — tab through chips in walkthrough; fix later = padding onto .dp-body; (3) .dp-chip:hover fires on read-only span chip — scope to button.dp-chip:hover, polish; (4) .pop .go/.x + .statuspick-wrap now zero-consumer dead CSS [plan-mandated STAY — owner adjudicates cleanup]; (5) max-height/overflow on .dp-body not card root — beneficial deviation, spec intent preserved, no action. Prior task minors (T1 assertion placement, T3 report line numbers) CARRY.
Walkthrough notes (static, no code change): tab order — ✕ precedes label input in DOM (autoFocus unaffected); caret math verified (~9.5px above card, covers top border); grid order matches spec diagram.
Manual visual check (plan T3 Step 6) DEFERRED to owner walkthrough — autonomous run.
Branch feat/dotpopover-redesign 4 commits 2d42810..a9864f1(+ledger), pushed to origin. NOT merged, NO PR — owner walkthrough decides.
OWNER WALKTHROUGH CHECKLIST (npx supabase start + npm run dev, /map):
[ ] Light theme: chips tinted + readable, Delete Dot text legible on pink tint
[ ] Dark theme: same card, tints follow; unmarked selected state readable (border carries it)
[ ] Caret tail points at dot; clamped popup at map edge = tail stays on card (accepted)
[ ] ✕ top-right closes; doesn't cover label text; Esc still closes
[ ] Chip click recolors + saves immediately (unchanged behavior)
[ ] Save/Lead/Job/Delete all work as before; forms titled "New lead"/"New job"
[ ] Small window: job form scrolls INSIDE the card, ✕ stays pinned
[ ] Cleaner: read-only card — static chip, label/notes, "Unlabeled dot" fallback, ✕ only
[ ] Phone/touch: all buttons ≥44px
[ ] MINOR-1 FEEL CHECK: mouse-click near top-right of label input — does ✕ hit area steal the click? (fix = coarse-scope the hit area; accept or order fix)
[ ] Keyboard: Tab through chips — focus rings clip at card edges (Minor-2, accept or order fix)

DOT PENDING-COMMIT WAVE (same branch feat/dotpopover-redesign, WAVE-BASE a9864f1, spec 38694ea, plan 7538ff5, owner-approved via Q&A: create-on-first-action / click-away-closes / 300px):
Task 1: complete (commit 4a71dee, pending-commit test contract — MapView no-create-on-click + click-away + chip-adopts-id guard [expected-green regression guard], DotPopover pending describe [chip create-report-update chain, failure alert, delete-discard]; reviewer verified all 5 reds genuine + 1 green genuine against live component source; 5 fail/15 pass; review approved. Minor: report misattributed failure-test's first failing assertion [doc nit, not code].)
Task 2: complete (commit d33eb02, DotPopover [PopDot id-nullable, onCreated, ensureId lazy create, remove() pending short-circuit, clamp 150] + MapView [OpenDot id|null + seq, sync onMapClick close-or-open-pending, absence rule id!=null guard, seq-keyed popup, onCreated adopts id + fresh + router.refresh, createError/createDot-import removed]; opus reviewer walked edge cases [double-create blocked by disabled=pending, pending survives props refresh, seq reuse safe across null render, late-resolve create can't resurrect closed popup]; 20/20 target, 273/273 full, tsc clean; review approved. Minors CARRY: (1) onMapClick/onPinClick not byte-verbatim to brief [verbose closures + redundant `as` cast, behavior identical, tsc clean — process drift]; (2) close-mid-commit race leaves an unlabeled created dot [brief-inherent, spec-consistent — walkthrough note].)
Task 3: complete (commit 4ed8c54, globals.css exactly 3 edits — pop-dot 260->300 [pairs with T2 clamp 150], dp-body + scrollbar-width thin + scrollbar-gutter stable, new .pop select 100% after .pop input; dp- block/color-mixes/statuspick/go/x byte-identical; battery 273/lint 0/tsc/build; review approved, zero findings.)
Task 4: complete. Closeout battery green at 6dff2d2 (controller-run): 274/274 unit (37 files), lint 0, tsc clean, build all routes. No DB changes — pgTAP untouched.
WAVE REVIEW (fable, a9864f1..4ed8c54 + fix re-verify): verdict READY FOR WALKTHROUGH at 6dff2d2. All 7 focus areas PASS (map click pure state both impls; seq keying + adoption; ensureId failure paths; absence/drawer/fresh handshakes; pending remove no-op; both observables test-enforced; CSS exactly 3 rules + clamp 150 = half of 300).
IMPORTANT FOUND+FIXED (commit 6dff2d2, TDD, reviewer CONFIRMED RESOLVED): onCreated adoption lacked pending guard — late createDot resolution could retarget a popup the user had moved to a SAVED dot (silent wrong-record writes). Fix: adopt only while prev.id == null; race pinned by controlled-promise test (last updateDot targets 7 not 99, deliberately tolerating the spec-accepted orphan write via toHaveBeenLastCalledWith).
MINORS (all CARRY): (1) T1 report doc nit; (2) T2 verbose onMapClick/onPinClick closures + dead `(prev as OpenDot|null)?.seq` cast [behavior identical — fold into next touch]; (3) close-mid-commit race leaves unlabeled orphan dot [spec-accepted — walkthrough note]; (4) adoption-guard MapView chip test wouldn't catch a key-regression remount [harden: type into label pre-chip, assert value survives]; (5) chip renders selected after failed create [matches pre-existing updateDot-failure pattern]; (6) residual pending->pending stale-id adoption race [bounded, self-evident, one-round-trip window; full fix needs popup-identity plumbing in onCreated].
Wave commits: 38694ea spec, 7538ff5 plan, 4a71dee tests, d33eb02 components, 4ed8c54 css, 6dff2d2 fix. Branch pushed. NOT merged, NO PR — owner walkthrough decides.
OWNER WALKTHROUGH CHECKLIST (pending-commit wave; npm run dev, /map admin):
[ ] Misclick on map -> popup opens, ✕/Esc/click-away -> NO dot anywhere (check /map after refresh)
[ ] Click-away while popup open: closes only; next click opens new popup
[ ] Chip on fresh click -> dot appears with that status, popup stays, edits survive
[ ] Save with label+notes on fresh click -> dot persisted with fields
[ ] Lead/Job on fresh click -> lead/job created, dot gone (converted), drawer opens
[ ] Offline/failure on first action -> error INSIDE popup, no ghost dot after retry
[ ] Existing dot click -> popup/edit/delete unchanged
[ ] Card 300px: forms breathe, scrollbar thin + beside inputs, selects full width (both forms)
[ ] Both themes still correct (no CSS regression in chips/caret)
[ ] KNOWN RACE (accepted): closing/moving popup the instant after a chip/Save click can leave an unlabeled dot — refresh shows it; delete if unwanted
HOTFIX (owner walkthrough crash, 2026-07-15, commit e38edbe): MapboxMap marker effect TypeError '.appendChild of undefined' at Marker.addTo. ROOT CAUSE (systematic-debugging, proven from mapbox-gl 3.25.0 source): Map.remove() sets _canvasContainer = void 0 (mapbox-gl-dev.js:106875) -> getCanvasContainer() undefined; construction-effect cleanup does created.remove() + setMap(null), but setMap(null) lands NEXT render while React flushes all cleanups before all creates in one commit -> marker/flyTo effects can re-run against the REMOVED map still in state. Triggers: dev Fast Refresh of map components (owner's tab was open on /map during wave edits) or token/interactive prop change (never happens in app -> PROD UNAFFECTED). The e92e733 rAF pattern protected construction only, not consumption. FIX (TDD): removed-map guard `!map.getCanvasContainer()` in marker + flyTo effects; new tests/unit/MapboxMap.stale.test.tsx reproduces the stale-commit interleave with a faithful mapbox mock (red = the exact production TypeError at MapboxMap.tsx:101); pre-existing MapboxMap.render.test.tsx FakeMap gained getCanvasContainer (+1 line, required by guard). Battery green at e38edbe: 275/275 unit (38 files), tsc clean, lint 0, build all routes.
OWNER WALKTHROUGH PASSED + MERGED TO MAIN 2026-07-15 (owner order "walkthrough passed, merge and clean": no-ff merge 7dd7c38, pushed 9a0248f..7dd7c38; feat/dotpopover-redesign deleted local+remote). Post-merge battery green on main: 275/275 unit (38 files), tsc clean, lint 0, build all routes. DOTPOPOVER REDESIGN + PENDING-COMMIT WAVE + MAPBOX HOTFIX FULLY CLOSED. Carried minors (redesign: dp-x mouse hit-area, focus-ring clip, span-chip hover, dead .pop .go/.x CSS; pending-commit: verbose seq closures, orphan-dot races x2, weak remount assertion, chip-selected-on-failed-create) remain open polish backlog — owner-accepted at walkthrough. NEXT per plan sequence: small-changes wave (7 tasks, plan 2026-07-14-small-changes.md, branch feat/small-changes; win-rate item's dots dependency now satisfied).

FULLSCREEN-MAP WAVE (branch feat/fullscreen-map off main 1e66e08, spec f6f228f, plan 1778d0f; owner-approved: edge-to-edge below topbar + floating toolbar/legend + cooperativeGestures removed):
Task 1: complete (commit 4862412, red contract — MapView .map-full root/no-h3/toolbar+legend-present test + MapboxMap FakeMap opts-capture + cooperativeGestures-undefined test; reviewer verified both reds genuine vs live source [root 'panel box map-panel' + h3 line 116; cooperativeGestures:true line 52]; 2 fail/13 pass; review approved, zero findings.)
Task 2: complete (commit 32dc3b8, MapView root map-full + h3 removed, MapboxMap cooperativeGestures deleted, globals.css fullscreen block replaced [.map-full negative-margin bleed 18/20 desktop 18/12 @860px, absolute-inset borderless map, floating maptools/legend w/ 82% card color-mix in-srgb + blur backdrop, z-15]; base .map/.maptools/.legend untouched [MiniMap safe]; 277/277 unit, lint 0, tsc, build; review approved, zero findings — byte-verbatim to brief.)
Task 3: complete. Closeout battery green at 32dc3b8 (controller-run): 277/277 unit (38 files), lint 0, tsc clean, build all routes. No DB changes.
WAVE REVIEW (fable, 1e66e08..32dc3b8): verdict READY FOR WALKTHROUGH. All 6 focus areas PASS (behavior-zero diffs verified line-level; MiniMap isolated [base .map intact, height prop wins]; margins match shell exactly incl. full @media enumeration — only 860px touches .app; color-mix in-srgb; map-panel zero live consumers; z-order pins 2 < toolbar 15 < pop 20 < drawer 50, no dropdown clipping). Statically-cleared: popup clamp better than before on full-width map, schematic fallback fills, no horizontal overflow, min-height 320 carried.
MINORS (no fixes required pre-walkthrough): (1) spec text named wrong dropdown class/z — CORRECTED in spec (commit follows; .searchbox-list z-30 flattens to 15 in maptools stacking context, popup may paint over open dropdown — accepted); (2) CARRY: -20px left bleed rides .app GAP not padding — coincidence gap==padding==20, comment it on next touch; (3) ACCEPTED: notched phones keep safe-area inset (bleed is flat -12px; content out of notch arguably correct — walkthrough note, not a bug); (4) cosmetic: cleaner/no-token toolbar sparse (DotCounts+chips only).
Wave commits: f6f228f spec, 1778d0f plan, 4862412 tests, 32dc3b8 impl, + spec correction. NOT merged, NO PR — owner walkthrough decides.
OWNER WALKTHROUGH CHECKLIST (fullscreen-map; npm run dev, /map):
[ ] /map: map edge-to-edge below topbar, both themes; no panel border/padding remnants
[ ] Desktop: scroll-wheel zooms WITHOUT Ctrl; drag pans
[ ] Phone: one-finger pan works; pinch zoom works; <=860px shell (hamburger) clean
[ ] NOTCHED PHONE: map stops at safe-area inset, not physical edge (accepted — flag if you want true bleed)
[ ] Toolbar floats top: search, dot counts, layer chips, hint readable over satellite imagery
[ ] Legend floats bottom-left, translucent, readable both themes
[ ] Popups (dot/pending) and drawers unchanged; popup may overlap toolbar/search dropdown (accepted)
[ ] Dashboard MiniMap unchanged (border, size, non-interactive)
[ ] No-token schematic fallback still fills the screen
WALKTHROUGH-FEEDBACK BATCH (owner-driven, same branch feat/fullscreen-map, controller-inline TDD where testable): 5b99d21 toolbar flush full-width top; 01bf313 DotPopover flips upward when yPct>50 (dp-up class + bottom caret, 2 render tests); 40abf42 compact phone popup @480px (38px targets [traded from 44 for map visibility — owner may veto], 260px, tighter spacing, 56vh cap); c023e72 mobile topbar single row (search+theme between hamburger and right-aligned ellipsized title, flex order @860px, .tbtitle class added); 36411a2 hamburger nav drawer narrowed (Drawer gains optional className, drawer-nav = min(280px,78vw); lead/job drawers untouched). Battery green at 36411a2: 279/279 unit (38 files), lint 0, tsc, build. Branch pushed. STILL NOT MERGED — owner continues walkthrough.

## 2026-07-16 — Small-changes wave Tasks 4-7 (branch feat/small-changes, plan 2026-07-14-small-changes.md)
Task 4: complete (commit de07b18, lib/calendar.ts pure month math [resolveMonth regex-validated, addMonths year-safe, monthWindow half-open [from,to), monthGrid UTC, bucketByDay slice(0,10) jobs-by-scheduled/leads-by-created, unscheduled skipped] + 14 unit tests; plan's test snippet guessed var(--sched) for unclaimed — actual jobStatusColor.unclaimed is var(--new), asserted real token per plan NOTE, + extra claimed-color pass-through test)
Task 5: complete (commit 224870a, /calendar server page [map-page scaffold lifted line-for-line: role-split month-scoped jobs (done INCLUDED, deleted excluded via base filter/views, unscheduled absent by gte), leads_public admin/rep-only (cleaners: fetch skipped entirely, not filtered), ?l= wins over ?j=, leadDetail admin/non-admin split, backTo=/calendar?m= so ?m= survives drawer open AND close] + CalendarGrid client grid [CHIP_CAP 3 + "+n more", day panel, phones-first @720px collapse to count dots] + nav item 05 all roles w/ renumber 06-10 + TITLES + calendar CSS; nav tests updated, 5 CalendarGrid render tests)
Task 6: complete (commit 602f21f, winRate(leads, noDots) → won/(won+lost+noDots), 0 on zero denominator; dashboard noDots = client filter of EXISTING dotsRes fetch, no new query; 4 test cases per plan)
Task 7: complete. Closeout battery green at 602f21f (controller-run): 307/307 unit (40 files), lint 0, tsc clean, build all routes incl. /calendar, npx supabase db reset clean (no schema changes this plan — sanity), pgTAP 305/305. Whole-branch review (7d48a38..602f21f) verdict ready-with-one-fix: Important — day panel state survived month nav (same-route reconcile keeps openDay; stale July panel over August grid) → FIXED edb51d8 key={month} remount (drawer open/close keeps same m so panel correctly persists there) + Space-key day-cell activation (a11y minor, plan-sourced omission). JS battery re-run green at edb51d8 (307/307, lint 0, tsc, build); DB battery not re-run — no DB-adjacent file touched post-pass. Minor deferred: day-panel heading shows raw ISO date (plan-verbatim, cosmetic — owner may veto at walkthrough); internal "pot" vocab lingers in comments/test names (owner-facing copy clean, zero "cleaner pot" hits in components/app/lib/tests); reviewer note: visibleJobs is a pass-through so "filters through visibleJobs" comments describe view-level protection (pre-existing, both map+calendar pages).
AWAITING owner walkthrough — do NOT merge. Walkthrough checklist (plan Task 7 Step 4): Cleaners Pay label view/edit + both no-pay confirms; streets style /map + MiniMap; faster search fly; locate button permission→blue dot (recenters until first pan — expected); calendar nav all roles, month grid, prev/today/next, chips open right drawer + Back keeps month, cleaner sees jobs only, phone collapse to dots + day panel; win rate drops when dots marked No, recovers on convert/delete.

## 2026-08-10 — Calendar-as-a-view wave (branch feat/small-changes, spec 73f7336, plan bb537e3)
Goal: retire standalone /calendar; Calendar becomes a third view mode (Board | List | Calendar) on /leads (leads by created date) and /jobs (jobs by scheduled date). No DB changes, no new queries — both host pages already fetch their full non-deleted set, so the grid buckets that with bucketByDay.
Task 1: complete (5d40d35, ViewToggle 2-state → 3-state ViewMode 'board'|'list'|'calendar'; board pushes bare base, list/calendar push ${base}?view=v, all {scroll:false}; Calendar deliberately omits ?m= so the button doubles as Today; 4 render tests; review clean).
Task 2: complete (630b7e8, CalendarGrid showLeads:boolean → kind:'lead'|'job' — one prop drives base path, chip param and hint, so the grid works under either host; everything from <div className="calgrid"> down untouched; test file rewritten to 8 tests covering per-host month-nav/chip hrefs, chip cap + "+n more", day panel; review clean. Left the intended single tsc error in app/(app)/calendar/page.tsx unpatched for Task 5, as planned).
Task 3: complete (c936c10, LeadsCalendarSection + ?view=calendar branch on /leads; scrhead action set mirrors LeadsListSection action-for-action [ViewToggle · History(admin) · Export CSV · + New lead(canEdit)] per consistency rule #1; Export stays whole-set not month-scoped [spec decision]; key={month} remount carried forward from edb51d8; backTo three-armed cal/list/board; Object.fromEntries — Maps don't cross the RSC boundary; 4 render tests; review clean).
Task 4: complete (9f5254e, JobsCalendarSection + ?view=calendar branch on /jobs; mirror of Task 3 with money-as-create-gate and useJobsRealtime() like JobsBoard/JobsListSection; buckets the already-computed `visible` set; drawer backTo made calendar-aware; ?deleted=1 History still short-circuits before the cal branch on both hosts; 4 render tests; review clean).
Task 5: complete (6e07f47, app/(app)/calendar/page.tsx deleted; /calendar dropped from NAV_ITEMS + TITLES and nav renumbered 01-09 no gaps; unused monthWindow() removed from lib/calendar.ts [its only caller was the page's month-scoped fetch]; nav + calendar tests follow, incl. 2 new single-kind bucketByDay cases; grep proves zero non-docs references to /calendar or monthWindow; review clean). Battery green at 6e07f47: 324/324 unit (43 files), lint 0, tsc CLEAN (the expected page error gone with the file), build succeeds with no /calendar route and both /leads and /jobs present. pgTAP not re-run — no DB file touched this plan. Note: hit the known Turbopack stale-.next gotcha on first tsc; rm -rf .next cleared it.
WHOLE-WAVE REVIEW (opus, bb537e3..6e07f47): verdict ready-for-walkthrough with doc fixes. Leads/jobs symmetry, URL/state coherence (toggle ↔ month nav ↔ chip deep-link ↔ drawer close ↔ + New ↔ ?deleted=1 precedence) and role gating all verified equivalent to board/list. Two Important findings, both documentation-shaped, FIXED in 384659c: (1) the new jobs-page comment claimed `visible` is role-filtered by visibleJobs — false, visibleJobs is a pass-through seam since the 2026-07-09 owner decision (every role sees all non-deleted jobs); real scoping is RLS + jobs_public. Same misstatement was already flagged on this branch 2026-07-16 and got re-introduced. (2) spec line + plan walkthrough checklist both said "cleaner sees only their own jobs" — would have made the owner report a false bug; corrected in both docs, the true half ("no History, no + New job") kept. Scoped re-review of 384659c: all findings ADDRESSED, no new breakage, deferred minors untouched.
MINORS DEFERRED (owner may veto at walkthrough): ViewToggle aria-pressed duplicates the cls() active predicate (cosmetic); act() stderr warning in createRoot render tests (pre-existing repo-wide, 8+ files — one-line vitest-setup fix if ever wanted, as its own change); `as Record<string, CalEntry[]>` cast on Object.fromEntries is confirmed unnecessary (plan-mandated text — bundle its removal with any future entries-shape change); CalendarGrid chip param comes from e.kind while base comes from host kind (latent only — single-kind hosts keep them agreed); calendar chips are next/link so opening a drawer scrolls to top where board/list use {scroll:false}; Calendar → History → Back lands on Board (same as List, pre-existing); day-panel heading still shows raw ISO date (carried from 2026-07-16).
BACKWARD COMPAT: bookmarked /calendar URLs now 404 — accepted, internal tool.
Wave commits: 73f7336 spec, bb537e3 plan, 5d40d35, 630b7e8, c936c10, 9f5254e, 6e07f47, 384659c. NOT merged, NO PR, NOT pushed — branch still owes the owner a walkthrough.
OWNER WALKTHROUGH CHECKLIST (calendar-as-a-view; npx supabase start, npm run dev; admin@ / rep@ / cleaner@clearview.dev, password123):
[ ] Nav has no Calendar item for admin, rep or cleaner; numbering reads 01-09 with no gap
[ ] /leads: toggle shows Board · List · Calendar; Calendar highlights when active
[ ] Leads calendar: leads sit on the day they were created, colored by status
[ ] Prev / Today / Next move months and stay inside /leads?view=calendar
[ ] Lead chip opens the LeadDrawer; closing returns to the same month, still in Calendar
[ ] + New lead from calendar opens the drawer; closing returns to the calendar month
[ ] Export CSV and History (admin) behave the same as in List view
[ ] /jobs: same toggle; jobs sit on their scheduled date; done jobs visible; unscheduled absent
[ ] Job chip opens the JobDrawer; close returns to the same month in Calendar
[ ] Cleaner on /jobs?view=calendar: sees the SAME job set as board/list (all non-deleted — 2026-07-09 decision), no History, no + New job
[ ] Phone width: cells collapse to count dots; tapping a day opens the day panel
[ ] Claim a job in a second window — the calendar refreshes on its own (realtime)
SUPERSEDES the "/calendar nav all roles, month grid, prev/today/next, chips..." line in the 2026-07-16 small-changes checklist — that page no longer exists; the rest of that checklist (Cleaners Pay, map streets/search/locate, win rate) still stands and remains partially walked (map + Cleaners Pay passed 2026-08-10).

## 2026-08-10 — OWNER ACCEPTED EVERYTHING; feat/small-changes MERGED TO MAIN
Owner order: "all work is accepted. push everything and merge to main and make main the source of truth." Walkthrough closed by acceptance (map + Cleaners Pay had passed; the rest accepted without a separate pass — the calendar checklist above stands as a record, not a gate).
Merged no-ff 50aa2cf (main 7d48a38..50aa2cf, 19 commits), pushed to origin. feat/small-changes deleted local + remote. MAIN IS NOW THE SOURCE OF TRUTH.
Contents: small-changes Tasks 1-7 (Cleaners Pay rename, streets-v12 + 2x flyTo, GeolocateControl, calendar lib, old /calendar page, win-rate No-dots, review fixes) + calendar-as-a-view wave (5 tasks) + its doc fixes + two mobile-topbar fixes (663ca4b toggle no longer wraps the bar to two rows; 51a083b search floors at 140px, title ellipsizes — both verified in headless Chrome 320-860px).
Battery green pre-merge at 51a083b and post-merge on main: lint 0, tsc clean, 327/327 unit (44 files), build all routes (no /calendar, /leads + /jobs present). pgTAP not re-run — no DB file touched since the last green run.
Carried minors remain open backlog (calendar wave list in the 2026-08-10 entry above; earlier waves' lists unchanged).
NEXT: branch exp/theme-directions cut from main 50aa2cf — theme experiments against the old AllSet-Rep-Portal palette (https://github.com/NotSlik/AllSet-Rep-Portal: dark #0b1220, violet #7c3aed -> sky #38bdf8 gradient, glass panels, sans/weight-950, big radii; vs our flat mono Blueprint+). Direction not yet chosen.

## 2026-08-10 — OWNER REQUEST BACKLOG (logged, NOT started) + theme-skin regressions
Branch `exp/theme-directions` (off main c5523a4). Theme work so far: 762e84c old-portal palette, f2ef5bd glass surfaces + sans type, 94714f9 drawer/form buttons, 52c8ae5 button-label overflow fix. Battery green at 52c8ae5 (lint 0, tsc, 327/327). NOT merged.

### 1. Live GPS on the map
Reps/cleaners see their own live position on /map (beyond the one-shot GeolocateControl shipped in 9ebb076 — that recenters until first pan and does not stream). Owner also asks, explicitly as an open question ("i dunno how hard it is"): can an ADMIN watch reps/cleaners live, for those who have sharing on?
Open questions for the spec: consent + opt-in UI; battery/permission story on phones; whether positions persist (new table + RLS) or are ephemeral broadcast over the existing realtime channel; retention; whether cleaners can see each other or only admins can see them. Treat admin tracking of staff as a privacy-sensitive feature — it needs an explicit, visible opt-in per user, not a silent default.

### 2. Create lead/job without a full customer — auto-create from the name alone
Today the lead/job create panel requires picking an existing customer. A rep standing in front of a prospect has no time to fill the full customer record. Wanted: type a NAME into the customer field; if no customer matches, creating the lead/job auto-creates a customer with just that name and links it, and the lookup then resolves to it.
Touches: CustomerLookup (components/customers/CustomerLookup.tsx), the lead + job create paths, and whatever server action inserts them. Watch: uniqueness/dedup on name, role gates (rep may create customers?), and not silently creating duplicates when the rep mistypes an existing name.

### 3. Red dot on customers missing phone or address + created-by traceability
Complement to #2: a shallow customer needs to be completed later. Red dot next to the customer name in the customers view when phone OR address is missing. Also surface WHO created the record, so an admin seeing many red dots can go back to that creator and get the gaps filled.
Touches: customers list + drawer, and a created_by column if one doesn't already exist (check before designing — this needs a migration + pgTAP if it doesn't). Dot must not rely on color alone (a11y): pair it with a title/aria-label such as "missing phone".

### 4. Background/contrast regressions from the glass skin (REPRO'D, cause known)
Owner: the map popup is transparent and unreadable, and a lead's service-type dropdown renders gray/hard to read — "double check with all drop down lists".
Cause is this branch's skin, confirmed in code, not a pre-existing bug:
  - `DotPopover` renders `pop box pop-dot` (components/map/DotPopover.tsx:35), and f2ef5bd made `.box` a translucent white-alpha gradient (`--surface`). Over map tiles it shows straight through. Fix: give popovers/menus an OPAQUE surface (`--card`) rather than `--surface`, or add a `.box--solid` variant for anything floating over imagery.
  - `input, select, textarea { background: var(--field) }` where `--field` is `rgba(0,0,0,.22)` dark / `rgba(255,255,255,.80)` light. Native `<option>` lists inherit the select's background, so translucency turns them muddy. Fix: opaque background on `select` and `option` in both themes.
  - Sweep every floating/overlay surface for the same problem: `.sresults` (global search), `.searchbox-list` (map search), `.caldaypanel`, drawers, and any menu that sits over the map.

### Backlog moved to a tracked file
All owner requests now live in `docs/owner-requests-backlog.md` (committed, 8 items: the 4 above plus delete-vs-deactivate for history-less customers, leads field-panes-to-windows [NEEDS CLARIFICATION — owner's wording is ambiguous], the near-invisible datetime-local calendar icon [cause: no `color-scheme` declared anywhere in globals.css, so native controls paint with light chrome in dark theme], and expenses rows having no detail/edit drawer [confirmed: ExpensesSection has a create drawer + row delete only]). This ledger keeps status; that file keeps what was asked and why.

---

## 2026-08-13 — FIRST PRODUCTION DEPLOY + item 4 (glass overlays) fixed

`exp/theme-directions` merged into main (no-ff), then the glass-skin overlay regression from item 4 above fixed on main as ee742df: `--field-solid` (the flattened equivalent of `--field` per theme) backs `select`/`option`, and `.pop` / `.sresults` override `.box`'s translucent `--surface` with the solid `--card`. The sweep found `.searchbox-list` and `.drawer` already opaque, and `.caldaypanel` in-flow rather than floating, so it keeps the glass. Battery green (327/327, clean prod build). Item 4 marked FIXED in `docs/owner-requests-backlog.md`; items 1-3 and 5-8 still open.

**Live: https://allset-crm.vercel.app** — Vercel project `allset-crm` (scope `all-set-crm`) against the existing Supabase Cloud project `gdetewfzxvmoyubfufzk`, which was paused on the free tier and resumed with an empty schema. All 29 migrations pushed with `supabase db push --db-url`; `seed.sql` deliberately NOT run, so production carries no demo customers or `password123` logins. Env: the three `NEXT_PUBLIC_*` vars plus `SUPABASE_SERVICE_ROLE_KEY`, on production and preview.

The first admin had to be bootstrapped out of band — the app has no signup page, and `createUser` is admin-gated, so nobody could have made the first account through the UI. Created via the service-role admin API exactly as `settings/actions.ts` does it (`auth.admin.createUser` with `email_confirm: true`, then the `profiles` row): `laith@allset.com`, role `admin`. Verified by requesting a password-grant token against the live project.

**Deploys are CLI-driven** (`npx vercel deploy --prod --yes`): `vercel link` failed to connect the GitHub repo because the Vercel GitHub app isn't installed on `osama-alawneh/AllSet-CRM`, so pushes to `main` do not deploy themselves.

**Still owed by the owner, dashboard-only:** the live auth config reports `disable_signup: false`, meaning anyone holding the publishable key — which ships in the browser bundle — can self-register (RLS still denies them data, since no profile row means no role); Site URL is unset; and both the DB password and the first admin password were pasted into a chat transcript and want rotating.

---

## 2026-08-13 (later) — navigation latency measured; fix list logged, NOT started

Owner reported navigation delay after the deploy, and his user noticed it too. Region move to `pdx1`
(commit `102d7be`) already halved page TTFB, ~880ms to ~490ms. The remainder was then measured
properly rather than guessed: a `/api/perf` probe on a preview deployment runs each phase of a page
render and returns the milliseconds, because `vercel logs` only samples and dropped most of the
timing lines. Rig lives on branch `perf/latency-probe` (pushed, NOT for main) — reuse it to verify
any fix.

Warm per navigation: `auth.getUser` 55ms → `auth.roleQuery` 25ms (serialised behind it) → the 9
parallel dashboard queries 80ms → render ~40ms → ~150-200ms of user-to-Oregon network. Cold roughly
doubles it. Ruled out: the app's query code (a bare `select id limit 1` costs 25-35ms, and a raw
fetch bypassing supabase-js costs the same — that is the Supabase floor) and connection setup (five
identical queries: 46, 25, 35, 27, 26ms).

The dominant cause is perceptual: no `loading.tsx` anywhere, so a click leaves the old page on
screen until the new one is fully ready, and Next will not prefetch dynamic routes without a loading
boundary. No caching of any kind exists — empty `next.config.ts`, no `use cache`, `no-store` on
every response.

Full findings and the ranked fix list are in `docs/owner-requests-backlog.md` item 10, split as the
owner asked: **1-3 easy/recommended/no-risk** (Suspense skeletons, the prefetching they unlock,
`staleTimes` client route cache) and **4-6 need care**, each with its specific hazard recorded —
auth bypass, stale permissions after a role change, and cross-user cache leakage under RLS. Owner
intends to execute this himself with superpowers, so the entry is written to be picked up cold.
