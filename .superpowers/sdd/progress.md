Task 1: complete (commits 820db51..5dc7955, scaffold; review: low-risk scaffold, verified inline)
Task 2: complete (commit 1968c60, schema; 3 pgTAP pass)
Task 3: complete (commit 39f7a85, RLS admin-only money; 5 pgTAP pass). auth_role() made SECURITY DEFINER to break recursion; granted select on invoices/items to authenticated.
MINOR (defer to Leads/Jobs plan): admins reading leads.quote_value/jobs.price need 'grant select on leads,jobs to authenticated' + a test (rep 0 rows via policy, admin sees). Not yet granted.
Task 4: complete (commit da5acd5, race-safe claim_job; 7 pgTAP pass)
Task 5: complete (commit 0af0926, supabase clients + auth helpers; adapted Next16 async cookies + vitest @ alias)
Task 6: complete (commit 9aca9c9, login + route guard; next build clean, 4 unit tests pass)
Task 7: complete (commit 814e197, seed + 3 login users; 7 pgTAP pass, test ids bumped to 900k to avoid seed collision)
FOUNDATION PLAN COMPLETE (Tasks 1-7). Logins: admin@/rep@/cleaner@clearview.dev pw password123.
FIX: commit f5335fc — grants migration 0004 + seed token fix; E2E verified (logins, RLS, claim-lock all pass). Removed stray CleanView PDF + temp verify script.
PLAN 2 (feat/customers): Task 1: complete (commit f685f24, customer write policies; pgTAP 12 assertions pass, review clean)
Task 2: complete (commits 574cfb7+6989399, Blueprint+ tokens/theme-cookie/shell + placeholders + /leads guard fix; 10/10 unit, build clean, review approved. Minor deferred to final review: layout getRole/getSession redundant round trips + user! assertion; guard duplication across invoices/settings/leads)
Task 3: complete (commit e1e2945, customers list page; 14/14 unit, build clean, review approved. Minor deferred: silent supabase error discard on list page, redundant getRole per page, tr keyboard a11y)
Task 4: complete (commits 259646c+b85f4a5, customer drawer edit/create + tabs + zero-row save fix; 17/17 unit, build clean, review approved. Minor deferred: ?c=0 truthy check, no not-found feedback, disabled-vs-readOnly a11y, unencoded tel/sms/mailto, untested zero-row error branch; pre-existing ThemeToggle lint error noted)
Task 5: complete (commits 56fb26b+a1cafd6, global typeahead + stale-response race fix; 21/21 unit, build clean, review approved. Minor deferred: supabase error discarded in GlobalSearch, no component-level race test)

PLAN 2 VERIFICATION (Task 6, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (5 migrations + seed applied clean).
- `npx supabase test db`: PASS (4 files: schema, rls_money, claim_job, customers_write; 12/12 assertions).
- `npm test`: PASS (21/21 unit tests, 7 files).
- `npm run build`: PASS (clean production build, Turbopack, 10 routes).
- Live drive against `npm run dev` (localhost:3000) + local Supabase, using injected `sb-127-auth-token` auth cookies (base64url-encoded session, `base64-` prefix, per @supabase/ssr default storageKey derived from host `127.0.0.1`) for admin/rep/cleaner sessions obtained via `POST /auth/v1/token?grant_type=password`. Created a local-only `.env.local` (gitignored, not committed) pointing at the local Supabase instance since none existed. 15/15 scripted checks PASS:
  1. admin `/customers`: Sarah Kim row + Invoices column header + per-row inv counts + sidebar Invoices/Settings — PASS.
  2. admin `/customers?c=1` (Sarah Kim) drawer: "CUSTOMER #" badge + "Jobs (" / "Invoices (" / "Leads (" tabs — PASS.
  3. rep `/customers`: no Invoices column, sidebar lacks Invoices/Settings — PASS.
  4. rep `/customers?c=1` drawer: no "Invoices (" tab, has "Save customer" button — PASS.
  5. cleaner `/customers?c=1` drawer: inputs disabled, no "Save customer" button — PASS.
  6. cleaner GET `/invoices` → 307 to `/dashboard` — PASS. rep GET `/invoices` → 307 to `/dashboard` — PASS. cleaner GET `/leads` → 307 to `/dashboard` — PASS.
  7. Unauthenticated GET `/customers` → 307 to `/login` — PASS.
  8. RLS mutation: rep PATCH `customers?id=eq.1` (own-scope write) → 200 + 1 row — PASS.
  9. RLS mutation: cleaner PATCH `customers?id=eq.1` → 200 + 0 rows (RLS-filtered, no error) — PASS.
  10. RLS mutation: cleaner INSERT into `customers` → 403 `42501` — PASS.
  11. Typeahead PostgREST `or()` filter for "sar" (rep token) → returns Sarah Kim (id 1, 142 Maple Ave, 555-0142) — PASS.
- All checks green. No app bugs found; only a test-script regex bug (React SSR comment node `<!-- -->` between number and text) caught and fixed during the run, not an app defect.
- Full command transcript and per-check detail: `.superpowers/sdd/task-6-report.md`. `verify-plan2.mjs` deleted after run per instructions (not committed).
Task 6: complete (commit 435aaaa verification + 7d58b06 lint fixes; pgTAP 12/12, unit 21/21, build clean, lint 0 errors, 15/15 live E2E checks pass). FINAL REVIEW: READY TO MERGE (opus whole-branch). Backlog carried: auth round-trip consolidation, requireRole helper, error UI for failed queries, tr keyboard a11y, ?c=0 latent, not-found drawer feedback, readOnly-vs-disabled, encodeURIComponent quick actions, race/zero-row test coverage, drawer key on customer switch. PLAN 2 COMPLETE.
PLAN 3 (feat/leads-map): Task 1: complete (commits f5c0c06+88c1321, migration 0006 lead writes + won->job trigger + pin RPC with NULL-safe role check + seed rework; pgTAP 24/24 across 5 files, review approved. Minor: trigger fires on no-op status writes — harmless)
Task 2: complete (commit 52224a1, lib/leads + lib/geo pure helpers; 36/36 unit, build clean, review approved, no findings)
Task 3: complete (commit 42056cc, kanban + setLeadStatus; 36/36 unit, build+lint clean, review approved. CARRIED TO TASK 4 (Important): click event fires after drag ends -> onOpen would pop drawer after every drag once ?l= consumer exists; fix in Task 4 via pointer-distance click suppression in LeadCard. Minor: no try/catch around action call in onDragEnd)
Task 4: complete (commits a78d708+6f53753, LeadDrawer + ?l= wiring on /leads + Task 3's carried click-suppression fix in LeadCard; 36/36 unit, build+lint clean. Verbatim brief, no deviations besides the mandated fix.)
Task 4: complete (commits a78d708+6f53753, LeadDrawer + ?l= wiring + post-drag click suppression; 36/36 unit, build+lint clean, review approved. Minor: downPos ref not reset after consume)
Task 5+6: complete (commits 62ae12c+5a5e63a+6ec49a8, map page schematic+mapbox-behind-token + pin RPC action + popover-dismiss fix; 36/36 unit, build+lint clean, review approved. Minor backlog: onPinClick useCallback/ref indirection, marker-sync vs map-recreate effect coupling, PinPopover default status 'won' product decision)

PLAN 3 VERIFICATION (rerun after fix, Task 7, full E2E pass against live local stack, 2026-07-02):
- First pass found a real blocker: reps could never restatus a lead — base leads SELECT is admin-only (protects quote_value) and Postgres requires SELECT-visibility for UPDATE...WHERE, so 0006's leads_update policy was dead code for reps; kanban drag / drawer buttons / won->job silently no-opped. pgTAP missed it (lives_ok passes on 0-row UPDATE). Fixed in f8c4c1e: 0007 set_lead_status SECURITY DEFINER RPC (NULL-safe role check, raises on 0 rows) + actions.ts routes through it; leads_map.sql extended with persistence read-backs.
- Rerun results (all green):
  - `npx supabase db reset`: PASS (7 migrations 0001-0007 + seed clean).
  - `npx supabase test db`: PASS (5 files: schema, rls_money, claim_job, customers_write, leads_map; 31/31 assertions).
  - `npm test`: PASS (36/36 unit, 10 files). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- Live drive (dev server :3000 + injected sb-127-auth-token cookies / bearer tokens, same method as Plan 2): 18/18 checks PASS:
  1. admin /leads: 4 kanban columns + customer names + $ quote on cards — PASS.
  2. rep /leads: renders, no $ amounts anywhere — PASS.
  3. cleaner /leads → 307 /dashboard — PASS.
  4. admin /leads?l=3 drawer: LEAD #0003 badge + $260 quote + status buttons — PASS. rep same: ••••• masked quote + status buttons — PASS.
  5. all roles /map: schematic .mpin pins (10 seeded); cleaner 200 view-only — PASS.
  6. cleaner /map?l=3: drawer present, NO status buttons, NO "Mark won" — PASS.
  7. rep RPC set_lead_status(7,'follow') → 204; admin GET confirms persisted — PASS. cleaner same → 400 P0001 "Not authorized to update leads" — PASS. rep on nonexistent lead → 400 P0001 "Lead 999999 not found" — PASS.
  8. won->job trigger: rep RPC set_lead_status(7,'won') → job created unclaimed; re-won → still exactly 1 job (idempotent) — PASS.
  9. pin RPC: rep create_lead_from_pin('E2E Pin','1 Test Way',42.33,-83.04,'new') → lead id; admin confirms customer+lead with coords — PASS. cleaner → 400 P0001 — PASS.
  10. admin /map pin count 10 → 11 after pin create — PASS.
- Only script-side fix during the run (not an app bug): restrict HTML matching to SSR body (exclude RSC flight payload — literal "$4" ref ids false-matched money regex) + normalize <!-- --> comment nodes (same artifact as Plan 2).
- DB reset to clean seed after run; verify-plan3.mjs deleted; dev server stopped. Full detail: .superpowers/sdd/task-7-report.md.
Task 7: complete (commit f8c4c1e fix + this ledger; pgTAP 31/31, unit 36/36, build clean, lint 0 errors, 18/18 live E2E checks pass). PLAN 3 VERIFIED.
Task 7 + FINAL: complete (verification rerun 18/18 live PASS after set_lead_status fix f8c4c1e; whole-branch review READY TO MERGE. PLAN 3 COMPLETE. Backlog: drop dead leads_insert/update policies+grant (inert defense-in-depth), Mapbox-path useCallback churn + marker/map effect coupling when token lands, onDragEnd try/catch)
PLAN 4 (feat/jobs): Task 1: complete (commit b4a4fd3, migrations 0008-0011 profiles read + claim_job role fix + set_job_status RPC + realtime ping trigger; pgTAP 44/44 across 6 files, review approved, only cosmetic nits)
Task 2: complete (commit 5d3d7df, lib/jobs pure helpers + canTransition matrix; 48/48 unit, build clean, review approved)
Task 3: complete (commit 8291638, jobs board/drawer/actions/page; 48/48 unit, build+lint clean, review approved. CARRIED TO TASK 4: disable claim/drag while action pending (double-click claim shows false 'already claimed' to owner); Minor: .claim.mine styling unused)
Task 4: complete (commits 0915830+9549a8e+ff29720, realtime wiring + pending gate + unmount-race guard; 48/48 unit, build+lint clean, review approved. Minor: board-wide pending disable accepted trade-off)

PLAN 4 VERIFICATION (Task 5, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (11 migrations 0001-0011 + seed clean). Seed jobs confirmed: lead1 claimed by cleaner(333), lead2 unclaimed, lead5 in_progress by cleaner(333), lead8 unclaimed.
- `npx supabase test db`: PASS (6 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board; 44/44 assertions).
- `npm test`: PASS (48/48 unit, 11 files). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- DB-layer RPC matrix (docker exec psql, no native psql on host — used `docker exec supabase_db_ClearViewCRM psql`): rep claim of unclaimed job -> `Not authorized to claim jobs` (blocked) — PASS. cleaner claim of unclaimed job -> returns claimed job row — PASS. cleaner `set_job_status` on OWN claimed job -> in_progress, persisted — PASS. cleaner `set_job_status` on a job they don't own (unclaimed, not theirs) -> `Job <id> not found or not yours` (blocked) — PASS. admin `set_job_status(...,'unclaimed')` on a job clears `claimed_by` to NULL — PASS. NOTE: the brief's own psql script resolves job ids via an inline subquery *inside* the `set role authenticated` cleaner/rep session; since the base `jobs` table's SELECT policy is admin-only (by design, protects `price`), that subquery silently returns NULL under non-admin roles and produces misleading "already claimed"/"NULL not found" errors that are artifacts of the test script, not the app. Fixed by resolving job ids via a superuser (RLS-bypassing) query first, then passing literal ids into the role-scoped calls — this is how the brief's own Step 3 script already does it. Documented here since it could trip up a future rerun of the raw brief text verbatim.
- Claim race (two concurrent `claim_job` calls, same unclaimed job, both cleaner-authenticated, backgrounded via `docker exec ... &`): exactly 1 of 2 logs contains `Job already claimed`; the other returned the claimed row; `claimed_by` set to the winner — PASS.
- Realtime smoke (`scripts/realtime-smoke.mjs`, deleted after run): admin client `setAuth()` + subscribed to private `channel('jobs',{config:{private:true}})`; cleaner client claimed an unclaimed job via RPC in parallel; admin received `broadcast{event:'change'}` with payload `{id:2,status:'claimed'}` — keys checked to be exactly `id,status` (no price/name leak) — PASS.
- Live drive (dev server :3000, cookie-injection `sb-127-auth-token` = `base64-` + base64url(session), same method as Plans 2/3; no browser-automation tool available in this environment so HTTP+cookie fetches against SSR'd pages were used, restricted to the SSR body excluding the RSC flight-payload script tags to avoid false-positive `$` matches): 18/18 checks PASS:
  1. admin `/jobs`: 4 column labels (Unclaimed/Claimed/In progress/Done), `$` price on cards, 🔒 locked pill on claimed job — PASS.
  2. rep `/jobs`: no `$` anywhere in SSR body, no `>Claim<` button — PASS.
  3. cleaner `/jobs`: no `$` anywhere; admin was made to claim lead8's job via RPC mid-run (seed only has one cleaner, so a genuinely "foreign-claimed" job was manufactured) — that job's customer name is absent from cleaner's board (visibleJobs correctly excludes it) — PASS.
  4. cleaner `/jobs?j=<own job>` renders a drawer (`role="dialog"`) with masked price (`•••••`) — PASS. cleaner `/jobs?j=<foreign job>` renders NO drawer — PASS.
  5. rep `/jobs?j=<unclaimed job>` renders a view-only drawer: no "Claim job" button, all status-transition buttons in the `statuspick` block carry `disabled` — PASS.
  6. admin `/leads` (regression check for the 0008 profiles-read-all policy change): still 200, still builds kanban HTML — PASS.
- Not independently re-driven as literal browser clicks/drags (no Playwright/Puppeteer tool available): claim-button click and drag-and-drop UI mechanics. These are thin wrappers — `app/(app)/jobs/actions.ts`'s `claimJob`/`setJobStatus` call `sb.rpc('claim_job'|'set_job_status', ...)` with no extra logic — over the exact RPCs proven correct (including the race and the full authorization matrix) at the DB layer above; `canTransition`/`groupJobsByStatus` drag-gating logic has its own 12/12 unit coverage in `tests/unit/jobs.test.ts`. Two-client live board sync was proven via the realtime smoke script (real supabase-js subscribe/broadcast, not a mock).
- DB reset to clean seed after the run; `verify-plan4.mjs` and `scripts/realtime-smoke.mjs` deleted; dev server process killed. No app bugs found — only the one test-script id-resolution issue documented above (not committed, caught and fixed during the run). Full detail: `.superpowers/sdd/task-5-report.md`.
Task 5: complete (verification pass; pgTAP 44/44, unit 48/48, build clean, lint 0 errors, DB RPC matrix 5/5, claim race 1/1, realtime smoke PASS, 18/18 live E2E checks pass). PLAN 4 VERIFIED.
Task 5 + FINAL: complete (verification 18/18 live + claim race + realtime smoke PASS; whole-branch review READY TO MERGE. PLAN 4 COMPLETE. Backlog: profiles role visible to all (least-priv option: definer RPC/projection), $0 price renders blank on admin card, cleaner backward moves allowed (product call), drawer non-optimistic + redundant refresh, stale 0004 comment, .claim.mine dead CSS)
PLAN 5 (feat/invoices): Task 1: complete (commit 9c297ac, migration 0012 invoice write grants + INV sequence + seed setval; pgTAP 51/51 across 7 files, review approved)
Task 2: complete (commit cfde374, lib/invoices pure helpers; 62/62 unit, build clean, review approved)
Task 3: complete (commit c8d2283, invoices page/table/drawer/print portal + saveInvoice; 62/62 unit, build+lint clean, review approved. Minor backlog: no key on InvoiceDrawer (relied on for create flow), items inputs not pending-disabled, zero-items reseed placeholder line)
Task 4: complete (commit b0aab32, createInvoiceFromJob + JobDrawer active button; 62/62 unit, build+lint clean, review approved)
Task 5: complete (commit e247062, lib/dashboard pure metrics; 73/73 unit total, review approved, date math hand-verified)
Task 6: complete (commit 5588bea feature + commit 03432a2 fix, role-aware dashboard KPIs/chart/claimable-jobs/mini-map + /dashboard revalidate + SchematicMap height prop; 73/73 unit, build+lint clean)

PLAN 5 VERIFICATION (Task 6, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (12 migrations 0001-0012 + seed clean).
- `npx supabase test db`: PASS (7 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board, invoices_write; 51/51 assertions).
- `npm test`: PASS (73/73 unit, 13 files, incl. jobs/invoices/dashboard suites). `npm run build`: PASS (clean, 10 routes). `npm run lint`: PASS (0 errors).
- REAL APP BUG found and fixed (within this task's own files, per instructions): the brief's verbatim `<KpiCountUp end={revenue} format={n => fmtMoney(Math.round(n))} />` passes an inline arrow function from the Server Component `app/(app)/dashboard/page.tsx` across the RSC boundary into the Client Component `KpiCountUp` — Next.js forbids non-serializable function props on that boundary (only Server Actions/plain data may cross), causing a 500 on every admin `/dashboard` load (`Functions cannot be passed directly to Client Components...`). Fixed in commit 03432a2: `format` is now a `'money'` string literal resolved by `KpiCountUp` itself via its own `fmtMoney` import; `page.tsx` passes `format="money"` and dropped its now-unused `fmtMoney` import. Re-verified clean (unit/build/lint + live).
- Lint fix (own file, applied inline before first commit): the brief's reduced-motion branch called `setVal(end)` synchronously in the effect body, tripping `react-hooks/set-state-in-effect`; wrapped it in a `requestAnimationFrame` callback (same visual result, satisfies the rule) since the animated path already only calls `setVal` from inside a rAF callback.
- DB-layer invoice matrix (docker exec psql, no native psql on host): admin insert with no `number` → default sequence-backed number assigned, item insert succeeds, rep insert blocked (`42501`), cleaner sees 0 invoices — all PASS mechanically. NOTE: the brief's literal expected value "INV-1004" does not hold when step 11 is run immediately after step 10's `npx supabase test db` (as the brief's own step order prescribes): `invoices_write.sql`'s pgTAP test consumes 2 values off the shared `invoice_number_seq` (one successful admin insert + one rep insert whose `number` DEFAULT is computed before its RLS `WITH CHECK` rejects the row) — Postgres does not roll back sequence advances on transaction ROLLBACK, so `pg_prove`'s per-file `begin;...rollback;` does not undo it. Confirmed empirically (sequence 1003→1005 across one `test db` run, main DB not an isolated shadow). Result was `INV-1006` instead of `INV-1004` — not an app defect, an artifact of running the psql matrix right after `test db` in the same persistent local DB; the generation mechanism (unique, monotonic, no collision with seeded INV-1001..1003) is correct. DB reset afterward per brief.
- Live drive (dev server :3000, cookie-injection `sb-127-auth-token` = `base64-` + base64url(session) via `POST /auth/v1/token?grant_type=password`, same method as Plans 2-4; no browser-automation tool available so canvas redraw-on-theme-toggle, drag/print-dialog, and literal button clicks were not independently driven — see note below): 45/45 scripted checks PASS, incl.:
  1. admin `/dashboard`: 200, Revenue·MTD + Overdue-invoices KPI cards present, `<canvas>` (RevenueChart) present, Jobs/week + Win rate + Claimable jobs + Neighborhood snapshot present, `$` figures present. KPI values cross-checked from the RSC flight payload against seed data: win rate 80% (4 won / 1 lost), overdue $165 (INV-1003: 140+25, issued 2026-05-28, >30d before server "today" 2026-07-02), jobs/week 3, revenue MTD $0 (correct: seed's only `paid` invoice, INV-1001, was issued in June, current month is July — `revenueMTD` is unit-tested to return 0 for this case, not a bug).
  2. rep + cleaner `/dashboard`: 200, NO "Revenue · MTD", NO "Overdue invoices", `•••••` placeholder present, NO `<canvas>`, **zero** `$[0-9]` matches in the SSR body, Jobs/week + Win rate + Claimable jobs still present — PASS for both roles.
  3. rep + cleaner GET `/invoices` → 307 to `/dashboard` — PASS.
  4. admin `/invoices`: 200, table lists INV-1001/1002/1003 — PASS.
  5. Invoice CRUD via PostgREST (admin bearer token): insert with no `number` → 201 + auto-assigned `INV-\d+` ≥ 1004 — PASS; insert `invoice_items` — PASS; read-back finds the row — PASS; PATCH status → 200 — PASS; DELETE item then invoice (cleanup) — PASS.
  6. Invoice denial via PostgREST: rep + cleaner insert → 403 (`42501`) — PASS; rep + cleaner select → 0 rows (RLS) — PASS for both.
  7. Claim-flow revalidate: cleaner `claim_job` RPC on an unclaimed job → success; `/dashboard` refetch reflects the change (claimable list changed) — PASS, confirming the new `revalidatePath('/dashboard')` addition in `claimJob`/`setJobStatus` is wired correctly end-to-end.
- Not independently browser-driven (no Playwright/Puppeteer tool in this environment, consistent with Plans 2-4's documented limitation): RevenueChart canvas redraw on theme toggle, KPI count-up animation + reduced-motion jump (both are thin rAF/DOM effects with their own unit-testable pure math — cubic ease-out — already covered; the underlying `end` prop values were cross-checked above via the RSC payload), InvoiceDrawer line-item editing/`+Add line`/status-badge-color JS interactions and the `🖨 Print PDF` browser print dialog (unchanged since Task 3, out of this task's diff surface), MiniMap pin-click → `/map?l=<id>` vs background-click → `/map` (both are trivial `router.push` calls wrapping already-proven navigation, no new logic).
- DB reset to clean seed after the run; `verify-plan5.mjs` and `check-kpi.mjs` deleted after use (not committed); dev server process killed. One real app bug found and fixed (RSC function-prop serialization, see above) plus one lint fix (set-state-in-effect), both within this task's own new files — no pre-existing app code touched. Full detail: `.superpowers/sdd/task-6-report.md`.
Task 6: complete (verification pass; pgTAP 51/51, unit 73/73, build clean, lint 0 errors, DB invoice matrix 4/4 mechanically correct (number-format deviation explained, not a defect), 45/45 live E2E checks pass). PLAN 5 VERIFIED.
Task 6: complete (commits 5588bea+03432a2+e675d01, dashboard page/components + revalidate tweaks + 45/45 live verification; 73/73 unit, pgTAP 51/51, build+lint clean, review approved. Minor: sequential awaits on dashboard page — brief-mandated)
FINAL: PLAN 5 READY TO MERGE (whole-branch review clean). Backlog: dashboard Promise.all, tax/deposit alignment between revenueMTD and invoiceTotal when Phase 3 lands, jobsThisWeek trailing-window semantics = product question for client, InvoiceDrawer key/pending-disable/placeholder-line nits. PLAN 5 COMPLETE.
PLAN 6 (feat/exports-pwa): Task 1: complete (commit 96261f8, role-aware CSV exports; 84/84 unit, build+lint clean, review approved, no findings)
Task 2: complete (commit 36a32b2, PWA manifest/icon/sw/register/offline; 84/84 unit, build+lint+prod-curl clean, review approved. Minor backlog: sw caches non-ok static responses until version bump; sw.js cache-control header note for prod deploy)
Task 3: complete (commits 28167f7+9c9229f, a11y pass + inner-control keydown guard + dead CSS removal; 84/84 unit, build+lint clean, review approved)
Task 4: complete (commit d62c5ca, dashboard Promise.all; 84/84 unit, build+lint clean, review approved)

PLAN 6 VERIFICATION (Task 5, full E2E pass against live local stack, 2026-07-02):
- `npx supabase db reset`: PASS (12 migrations 0001-0012 + seed clean; no DB objects changed this plan).
- `npx supabase test db`: PASS (7 files: schema, rls_money, claim_job, customers_write, leads_map, jobs_board, invoices_write; 51/51 assertions — no regression).
- `npm test`: PASS (84/84 unit, 14 files). `npm run build`: PASS (clean, 10 routes incl. /manifest.webmanifest, /offline). `npm run lint`: PASS (0 errors).
- `npx vitest run tests/unit/csv.test.ts`: PASS (11/11) — the authoritative role-omission specs all green: leadsCsvTable/jobsCsvTable/customersCsvTable include Value/Price/Invoices only for admin (header+cell both omitted for non-admin, not blanked); invoicesCsvTable Total spec; csvEscape formula-injection guard; toCSV BOM+CRLF.
- Headless PWA probes (`npm run build && npm run start`, curl): `/manifest.webmanifest` 200 with `ClearView CRM`, `display:standalone`, `theme_color:#2f6df6`, icons array — PASS. `/sw.js` contains all 4 allowlist markers (`clearview-v1`,`navigate`,`/_next/static/`,`/offline`) — PASS. Manual review of the full sw.js confirmed the default-deny contract intact: non-GET early-return, cross-origin early-return, navigate network-only w/ `/offline` catch-fallback (HTML never cached), static-asset allowlist cache-first, else no interception. `/offline` 200, `/icon.svg` 200 `image/svg+xml` — PASS.
- Live CSV drive: Export is 100% client-side (`downloadCSV`/`toCSV` called directly in the table components, no server route) — no browser-automation tool available (same limitation as Plans 2-5) to drive an actual click->save->open-in-Excel round trip. Verified as a proxy: `⬇ Export CSV` button present in SSR HTML for every role/route that should show it (admin/rep/cleaner customers+jobs, admin/rep leads, admin invoices), combined with the Step 2 unit-tested builder role-omission logic — treated as equivalent coverage per prior-plan precedent for file-download flows.
- Live regression sweep (`verify-plan6.mjs`, ad-hoc/deleted, dev server + cookie-injected sb-127-auth-token sessions, same method as Plans 2-5): 93/93 scripted checks PASS — every route 200s for permitted roles; rep/cleaner `/invoices`+`/settings`->307 `/dashboard`; cleaner `/leads`->307 `/dashboard`; zero `$[0-9]` leaks in rep/cleaner dashboard/customers/leads/jobs SSR bodies (RSC flight payload stripped to avoid false positives); cleaner dashboard shows `•••••` mask + no Revenue-MTD/Overdue KPI cards (rep same, no mask check needed); admin dashboard has `<canvas>` + `$` KPIs, admin leads/jobs show `$` values; skip-link + `#main` landmark present on every route; `aria-current="page"` on active nav for all 3 roles; `aria-pressed` confirmed on LeadDrawer (admin `/leads?l=3`) and JobDrawer (admin `/jobs?j=1`) statuspick buttons (1 true + 3 false each).
- Not independently browser-driven (no Playwright/Puppeteer tool, consistent with every prior plan): literal Tab-key skip-link reveal, real Enter/Space row activation on customers/invoices tables (onKeyDown handlers invisible to SSR HTML, unchanged since Task 3, no new logic this task).
- RESIDUAL MANUAL CHECKS (browser-only, documented for the user, non-blocking): (1) Chrome DevTools -> Application -> Manifest shows "ClearView CRM" + installable; (2) Service Workers panel shows `/sw.js` activated; (3) DevTools "Offline" toggle + navigate renders `/offline`; (4) "Add to Home Screen" installs a standalone window; (5) iOS Safari ignores the SVG manifest icon — `apple-touch-icon` PNG is a documented post-MVP follow-up, not added here.
- No app bugs found; no application code touched. DB reset to clean seed after the run; dev/prod server processes killed; verify-plan6.mjs (scratchpad, not committed) deleted from working consideration. Full detail: `.superpowers/sdd/task-5-report.md`.
Task 5: complete (verification pass; pgTAP 51/51, unit 84/84, build clean, lint 0 errors, csv role specs 11/11, headless PWA probes clean, 93/93 live E2E checks pass). PLAN 6 VERIFIED. MVP COMPLETE (Plans 1-6 all verified).
Task 5 + FINAL: complete (93/93 live + PWA headless checks; whole-branch review READY TO MERGE. PLAN 6 COMPLETE — MVP DONE. Backlog: sw res.ok guard, sw.js Cache-Control at deploy, csvEscape prefixes any leading -/+/=/@ value not just injection)

PHASE 1.5 — PLAN 7 (feat/auth-admin):
Task 1: complete (commit 73a86b5, Blueprint+ login + dark theme default flip; 84/84 unit, lint+build clean, review approved, no findings)
Task 2: complete (commit b2d3ec3, sidebar sign-out; 84/84, lint+build clean, review approved. Minor backlog: SignOutButton stuck-pending if auth.signOut() rejects — plan-mandated verbatim code, add try/finally or unconditional redirect)
Task 3: complete (commit e6e1fae, parseNewUserForm TDD; 89/89, lint+build clean, review approved, no findings)
Task 4: complete (commits c6d9655+ea91a07, service-role client + createUser/setUserRole actions + orphan-rollback fix; 89/89, lint+build clean, review approved after fix. Minor backlog: opaque non-null env assertions in lib/supabase/admin.ts)
Task 5: complete (commits f1c4703+499d6e5, settings users page + UsersPanel + migration 0013 service_role profiles grants — live check caught real runtime permission bug in T4 actions, migration was the fix; 89/89, lint+build clean, 12/12 live checks, review approved. NOTE: plan 8 task 1 migration must renumber to 0014)
Task 6 + FINAL: complete (fable whole-branch review READY TO MERGE; server-only guard added 63cad36; merged to main. Backlog: pgTAP grant assertions, normalizeRole on settings page p.role, ThemeToggle SSR label wrinkle, SignOutButton reject path, last-admin protection, listUsers 200 cap. PLAN 7 COMPLETE)

PLAN 8 (feat/crud-datamodel):
Task 1: complete (commit 5b2650c, migration 0014 [renumbered from brief 0013] + crud_rpcs pgTAP; pgTAP 80/80, 89/89 unit, lint+build clean, review approved. Minor backlog: delete not-found raise unasserted in pgTAP; set_updated_at not definer [harmless])
Task 2: complete (commits 76df4bb+ef9b260, Tabs type=button root-cause fix + minirow deep-links + keyboard a11y rowNav; 89/89, lint+build clean, review approved after a11y fix)
Task 3: complete (commit ed9035e, Lead/Job types + parseLeadForm/parseJobForm TDD + CSV Description column + build-forced select/literal threading through dashboard/jobs/leads/map pages [reviewed: no logic change, no money leak]; 98/98, lint+build clean, review approved. Minor backlog: parseJobForm date format-only validation)
Task 4: complete (commit e09be12, six lead/job CRUD server actions wrapping 0014 RPCs, revalidatePaths verified, param names cross-checked to migration; 98/98, lint+build clean, review approved, no findings)
Task 5 + FINAL: complete (fable whole-branch review READY TO MERGE, merged to main. Backlog: PRE-EXISTING 0006 grant gap — rep can blind-write quote_value via direct PostgREST insert/update on base leads (no read-back possible; integrity not leak) → follow-up migration to column-scope grants; jobs/actions.ts money comment overstates parser role; won-job description not re-synced on lead edit; pgTAP roleless coverage for job RPCs; delete_job invoice SET NULL untested. PLAN 8 COMPLETE)

PLAN 9 (feat/drawer-ux):
Task 1: complete (commit c3a64fc, LeadDrawer read/edit/create/delete; 98/98, lint+build clean, 26/26 SSR checks, review approved. Minor backlog: edit-form Description placement [plan-mandated], LeadDrawer missing key on lead switch [latent])
Task 2: complete (commit 4853af7, JobDrawer details+lead quick-view+admin CRUD; 98/98, lint+build clean, live per-role SSR pass, review approved. Minor backlog: $0 renders as dash [pre-existing]; seed jobs null description [seed-only])
Task 3: complete (commit 20b67ab, CustomerDrawer read-only default + Edit toggle; 98/98, lint+build clean, live SSR pass, review approved. Controller-resolved warning: cleaner ?new=1 shows create form but RLS [0005] blocks insert — UX nit backlog. Minor: raw enum type display [plan-mandated])
Task 4: complete (commit 15df1f8, InvoiceDrawer placeholders + read-only mode + empty-line drop rule; 99/99, lint+build clean, review approved. Minor backlog: no router.refresh after edit-save [plan-mandated], no remove-line control [pre-existing])
Task 5 + FINAL: complete (battery green [pgTAP 80/80, unit 99/99, lint, build], 67/67 live walkthrough, fable review WITH FIXES -> drawer key fix 891b49b applied, merged to main. Backlog: customers ?new=1 cleaner gate, invoices isNew&&!selected, admin cannot clear money via forms [RPC coalesce semantics], enum label + $0 display, JobDrawer/LeadDrawer minirow keyboard nav, won-job desc resync. PLAN 9 COMPLETE)

PLAN 10 (feat/search-views):
Task 1: complete (commit e163e73, buildEntityOrFilter/filterLeads/filterJobs/filterInvoices/SearchHit/hitHref TDD; 106/106, lint+build clean, review approved, no findings)
Task 2: complete (commit 97ba38d, grouped role-aware global search, 4-way fanout w/ shared cancel token; 106/106, lint+build clean, live RLS role checks, review approved. Minor note: cleaner leads gate client-side only [money-free view, pre-existing architecture])
Task 3: complete (commit 381d32b, local filters on leads/jobs boards + invoices table; CSV full-export + drag-state integrity verified; 106/106, lint+build clean, review approved. Minor backlog: empty-state wording, kanban no-match state)
Task 4: complete (commit 8e560f0, board/list toggle + LeadsListTable/JobsListTable + view-preserving backTo; 106/106, lint+build clean, live role checks, review approved. Minor backlog: useJobsRealtime hook extraction [plan-mandated copy], th scope=col, table captions)
Task 5 + FINAL: complete (battery green [pgTAP 80/80, unit 106/106, lint, build], live 3-role walkthrough PASS, fable review READY TO MERGE no fixes, merged to main. Backlog: dropdown reopen after dismiss, Enter stale first-hit, buildEntityOrFilter empty-fields guard, search hits drop ?view=list, leads not searchable by customer name [plan choice], fmt/day dedup, pgTAP view-money-column guard assertion, useJobsRealtime hook, th scope/captions. PLAN 10 COMPLETE — PHASE 1.5 DONE, ALL 4 PLANS MERGED)

HARDENING WAVE (feat/hardening, post-phase-1.5):
Hardening: complete (commit 18262fa merged, migration 0015 revokes table-level leads insert/update, re-grants all columns except quote_value+id; pgTAP 84/84 [+4 guards: views have no money columns, rep 42501 on quote_value write], 106/106 unit, lint+build clean, review approved. Minor backlog: same-file positive-control lives_ok in rls_money.sql; status grant kept for leads_map.sql legacy test)
Polish wave: complete (commits 4661115..ee98b52 merged — useJobsRealtime extraction, th scope+aria-label, conditional empty states, customers new=1 cleaner gate, invoices isNew&&!selected; 106/106, lint+build clean, review approved, no findings)
Money-clear: complete (commit 907bc2d merged, blankMoneyToZero form-boundary helper, user-decided semantics; 111/111, lint+build clean, RPC live check, review approved. Minor backlog: money-form test name over-claims parser integration)
User decisions 2026-07-06 (2nd batch): blank money = clear to 0 [done]; won-job description stays copy-on-create [no change]; search hits keep board target [no change].
Cookie-refresh fix: complete (commit 04dbaed merged — proxy.ts session refresh [Next 16 renamed middleware->proxy] + setAll try/catch; repro BEFORE 500/AFTER 200 with rotated refresh token; 111/111, lint+build clean, PWA assets intact, review approved. User-found bug during test run: stale browser session hit refresh path our fresh-session live checks never exercised. Minor backlog: anchor matcher lookahead terms)
WAVE 1 (branch wave1-security-perf-pwa): Task 1: complete (commits 61ebb6c..e604dd8, SEC-1..4 migration 0016 + pgTAP pins; review clean). Minor deferred to final review: SEC-4 pins only created_by (not created_at/updated_at); no explicit rep/admin branch assertion for jobs_public.
Task 2: complete (commit 6343f2a, cache() auth + parallel page queries + logQueryError; 111/111, build clean, review approved). Minor deferred to final review: getRole logs via raw console.error not logQueryError (plan-mandated); customers drawer detail reads (page.tsx:84-107) left sequential though independent.
Task 3: complete (commits a65a721+07bfaa8, iOS PWA icons/meta/manifest + legacy apple-capable tag via metadata.other; 111/111, build clean, review approved, no open findings).
WAVE 1 COMPLETE (5 commits 61ebb6c..07bfaa8 on wave1-security-perf-pwa). Exit checklist all green: pgTAP 8/8 (91 asserts), vitest 111/111, lint+tsc+build clean, REST claim_job as cleaner returned bare id, jobs_public money-free + row-filtered, manifest JSON valid w/ PNG+maskable, served HTML has apple-mobile-web-app-capable + viewport-fit=cover. Final whole-branch review (fable): ready to merge, 0 Critical/Important.
Wave 2 riders from final review: rep-arm pgTAP assertion for jobs_public (only untested branch of view predicate); logQueryError sweep for dashboard/settings pages; themeColor unconditional dark vs cookie light theme; maskable icon 80%-width square may clip circular mask (check DevTools preview); fold getRole console.error into logQueryError when touching lib/auth.ts.
NOTE: local Supabase 54xxx ports blocked by Windows excluded-port ranges (54235-54435); stack currently runs on 55xxx while config.toml says 54xxx — remap config temporarily for test:db, revert before commit (or reboot/winnat reset to reclaim 54xxx).
